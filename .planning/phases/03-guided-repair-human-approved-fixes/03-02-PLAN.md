---
phase: 03-guided-repair-human-approved-fixes
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified: [agent-rs/Cargo.toml, agent-rs/src/agent_core/repair/tools.rs, agent-rs/src/tools.rs, agent-rs/src/security/policy.rs, agent-rs/src/agent_core/repair/mod.rs]
autonomous: true
requirements: [FIX-01]

must_haves:
  truths:
    - "Services can be started/stopped/restarted by the agent"
    - "Packages can be installed/updated/removed by the agent"
    - "Permissions (chmod/chown) can be modified by the agent"
    - "Policy engine permits these actions only with user approval"
  artifacts:
    - path: "agent-rs/src/agent_core/repair/tools.rs"
      provides: "ServiceRepairTool, PackageRepairTool, PermissionRepairTool"
  key_links:
    - from: "agent-rs/src/agent_core/repair/tools.rs"
      to: "service-manager crate"
      via: "ServiceManager implementation"
    - from: "agent-rs/src/security/policy.rs"
      to: "agent-rs/src/agent_core/tool_runtime.rs"
      via: "PolicyDecision::RequireApproval return"
---

<objective>
Implement core repair tools for services, packages, and file permissions with Human-in-the-Loop (HITL) integration.

Purpose: To enable the agent to fix common OS issues using standard system management tools.
Output: ServiceRepairTool, PackageRepairTool, and PermissionRepairTool.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/03-guided-repair-human-approved-fixes/03-RESEARCH.md
@agent-rs/src/tools.rs
@agent-rs/src/security/policy.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Service Repair Tool & Update Repair Module</name>
  <files>agent-rs/Cargo.toml, agent-rs/src/agent_core/repair/tools.rs, agent-rs/src/agent_core/repair/mod.rs, agent-rs/src/tools.rs</files>
  <action>
    - Add `service-manager = "0.11"` to agent-rs/Cargo.toml.
    - In `agent-rs/src/agent_core/repair/mod.rs`, add `pub mod tools;`.
    - Create `agent-rs/src/agent_core/repair/tools.rs` and implement `ServiceRepairTool`.
    - Support `start`, `stop`, and `restart` operations using the `service-manager` crate.
    - Register `service_repair` tool in `agent-rs/src/tools.rs`.
    - Ensure the tool is categorized such that it triggers the HITL approval gate.
  </action>
  <verify>
    <automated>cargo test agent_core::repair::tools::service</automated>
  </verify>
  <done>Agent can manage system services after human approval. Tools module is declared in repair/mod.rs.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Package Repair Tool</name>
  <files>agent-rs/src/agent_core/repair/tools.rs, agent-rs/src/tools.rs</files>
  <action>
    - Implement `PackageRepairTool` in `repair/tools.rs`.
    - Support `apt` (Debian/Ubuntu), `dnf` (Fedora/RHEL), and `choco` (Windows).
    - Features: `install`, `remove`, `update`.
    - Mandatory: Implement `--dry-run` check and lock-file detection (e.g., `/var/lib/dpkg/lock`) before execution.
    - Register `package_repair` tool in `agent-rs/src/tools.rs`.
  </action>
  <verify>
    <automated>cargo test agent_core::repair::tools::package</automated>
  </verify>
  <done>Agent can install or update packages with safety pre-checks and human approval.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Permission Repair Tool and Policy Updates</name>
  <files>agent-rs/src/agent_core/repair/tools.rs, agent-rs/src/security/policy.rs</files>
  <action>
    - Implement `PermissionRepairTool` for fixing file/directory permissions (chmod/chown).
    - Update `agent-rs/src/security/policy.rs`:
        - Move `chmod`, `chown`, `systemctl` from `DANGEROUS_COMMANDS` to a new `MEDIUM_RISK_COMMANDS` list (or update `is_medium_risk_command`).
        - Ensure these commands now return `PolicyDecision::RequireApproval` instead of `PolicyDecision::Deny`.
  </action>
  <verify>
    <automated>cargo test security::policy::tests::risk</automated>
  </verify>
  <done>Policy allows repair commands under human supervision, and the PermissionRepairTool is operational.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Tool -> Package Manager | Direct interaction with system package managers. |
| Tool -> Service Manager | Interaction with init systems (systemd, etc.). |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02 | Elevation of Privilege | PackageRepairTool | mitigate | Use `privilege` crate to ensure escalation only happens during the actual execution of the approved command. |
| T-03-02 | Tampering | PackageRepairTool | mitigate | Always use `--dry-run` and present the impact (number of changes) to the user during the approval gate. |
</threat_model>

<verification>
Ensure all repair tools are registered and their tests pass. Verify that `chmod` now triggers approval instead of denial.
</verification>

<success_criteria>
- `ServiceRepairTool`, `PackageRepairTool`, and `PermissionRepairTool` are implemented and registered.
- System-modifying commands are moved from "Blocked" to "Approval Required".
- Dry-run and lock detection are implemented for package management.
</success_criteria>

<output>
After completion, create `.planning/phases/03-guided-repair-human-approved-fixes/03-02-SUMMARY.md`
</output>
