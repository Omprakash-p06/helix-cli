---
phase: 03-guided-repair-human-approved-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [agent-rs/Cargo.toml, agent-rs/src/types.rs, agent-rs/src/tui/approval.rs, agent-rs/src/agent_core/repair/snapshots.rs, agent-rs/src/agent_core/tool_runtime.rs, agent-rs/src/agent_core/mod.rs, agent-rs/src/agent_core/repair/mod.rs]
autonomous: true
requirements: [FIX-01, FIX-02]

must_haves:
  truths:
    - "User is prompted for confirmation before any repair tool executes"
    - "System state can be captured in a snapshot before modification"
    - "The agent waits for user input and respects the decision (Allow/Deny)"
  artifacts:
    - path: "agent-rs/src/types.rs"
      provides: "PermissionRequester trait and Request/Response types"
    - path: "agent-rs/src/tui/approval.rs"
      provides: "InquirePermissionRequester implementation"
    - path: "agent-rs/src/agent_core/repair/snapshots.rs"
      provides: "SnapshotManager implementation"
  key_links:
    - from: "agent-rs/src/agent_core/tool_runtime.rs"
      to: "agent-rs/src/types.rs"
      via: "PermissionRequester::request_permission call"
    - from: "agent-rs/src/tui/approval.rs"
      to: "inquire crate"
      via: "Confirm::prompt"
---

<objective>
Implement the Human-in-the-Loop (HITL) permission system and core snapshot management for system repairs.

Purpose: To ensure no state-modifying action is taken without human approval and that a restorable system state exists before any repair attempt.
Output: PermissionRequester trait, TUI approval implementation, and SnapshotManager.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/03-guided-repair-human-approved-fixes/03-RESEARCH.md
@agent-rs/src/agent_core/tool_runtime.rs
@agent-rs/src/security/policy.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement HITL Permission System & Mock Tests</name>
  <files>agent-rs/Cargo.toml, agent-rs/src/types.rs, agent-rs/src/tui/approval.rs</files>
  <action>
    - Add `inquire = "0.9"` to agent-rs/Cargo.toml.
    - In `agent-rs/src/types.rs`, define `PermissionRequest` (tool name, arguments, reason) and `PermissionResponse` (allow/deny).
    - Define `PermissionRequester` trait with an async `request_permission` method.
    - Implement `InquirePermissionRequester` in `agent-rs/src/tui/approval.rs` using `inquire::Confirm`. Use `tokio::task::spawn_blocking` for the sync UI.
    - **UI Interaction Verification:** Add unit tests using `mockall` or a custom mock `PermissionRequester` to verify that the agent's logic correctly branches based on user response (Allow vs Deny).
  </action>
  <verify>
    <automated>cargo test tui::approval::test_permission_requester_mock</automated>
  </verify>
  <done>PermissionRequester trait is implemented and verified with mock interaction tests.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Snapshot Management & Repair Module</name>
  <files>agent-rs/src/agent_core/repair/snapshots.rs, agent-rs/src/agent_core/repair/mod.rs, agent-rs/src/agent_core/mod.rs</files>
  <action>
    - Create `agent-rs/src/agent_core/repair/mod.rs`.
    - In `mod.rs`, declare `pub mod snapshots;`.
    - Update `agent-rs/src/agent_core/mod.rs` to export `repair`.
    - In `snapshots.rs`, implement `SnapshotManager` with `create_snapshot` and `restore_snapshot`.
    - For Windows: use `Command` to call `vssadmin create shadow /for=C:`.
    - For Linux: implement a simple `rsync` based backup of `/etc` and other critical paths as a fallback.
  </action>
  <verify>
    <automated>cargo test agent_core::repair::snapshots</automated>
  </verify>
  <done>SnapshotManager can create and track system snapshots or configuration backups. Repair module is initialized.</done>
</task>

<task type="auto">
  <name>Task 3: Integrate HITL into ToolRuntime</name>
  <files>agent-rs/src/agent_core/tool_runtime.rs</files>
  <action>
    - Update `ToolRuntime` to store an `Arc<dyn PermissionRequester>`.
    - In `execute_sync`, when `PolicyDecision::RequireApproval` is encountered, call `requester.request_permission`.
    - If permission is granted, proceed with execution; otherwise, return a `ToolResult` indicating denial by user.
    - Add a test case `test_hitl_interception` ensuring `ToolRuntime` actually calls the requester.
  </action>
  <verify>
    <automated>cargo test agent_core::tool_runtime::test_hitl_interception</automated>
  </verify>
  <done>ToolRuntime successfully intercepts "RequireApproval" decisions and waits for user input.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Agent -> OS | State-modifying tools (snapshots, repairs) cross this boundary with elevated privileges. |
| User -> Agent | Human-in-the-loop gate ensures user controls repair execution. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Elevation of Privilege | PermissionRequester | mitigate | Implement strict input sanitization on permission request messages to prevent display-based injection. |
| T-03-01 | Denial of Service | SnapshotManager | mitigate | Verify disk space before snapshot creation in pre-flight checks (implemented in later plan). |
</threat_model>

<verification>
Verify that `cargo test` passes and that the new `PermissionRequester` can be initialized in the TUI.
</verification>

<success_criteria>
- `PermissionRequester` trait exists and has a working TUI implementation.
- `SnapshotManager` can invoke `vssadmin` or `rsync` based on the platform.
- `ToolRuntime` successfully intercepts "RequireApproval" decisions and waits for user input.
</success_criteria>

<output>
After completion, create `.planning/phases/03-guided-repair-human-approved-fixes/03-01-SUMMARY.md`
</output>
