---
phase: 03-guided-repair-human-approved-fixes
plan: 03
type: execute
wave: 3
depends_on: [03-02]
files_modified: [agent-rs/src/agent_core/repair/workflow.rs, agent-rs/src/agent_core/repair/scoring.rs, agent-rs/src/agent_core/diagnostics/reasoning.rs, agent-rs/src/agent_core/tool_runtime.rs, agent-rs/src/agent_core/mod.rs, agent-rs/src/agent_core/repair/mod.rs]
autonomous: true
requirements: [FIX-02, FIX-03]

must_haves:
  truths:
    - "Repairs are automatically rolled back if validation fails"
    - "Agent shows a warning if its confidence in a fix is below 80%"
    - "Confidence scores are calculated using token probabilities and evidence coverage"
  artifacts:
    - path: "agent-rs/src/agent_core/repair/workflow.rs"
      provides: "SafetyLoop implementation with rollback"
    - path: "agent-rs/src/agent_core/repair/scoring.rs"
      provides: "Bayesian confidence scoring logic"
  key_links:
    - from: "agent-rs/src/agent_core/repair/workflow.rs"
      to: "agent-rs/src/agent_core/repair/snapshots.rs"
      via: "Automatic rollback on validation failure"
    - from: "agent-rs/src/agent_core/repair/workflow.rs"
      to: "agent-rs/src/types.rs"
      via: "Injecting low-confidence warnings into PermissionRequest"
---

<objective>
Finalize the transactional safety loop and implement Bayesian confidence scoring for diagnostic hypotheses.

Purpose: To ensure repairs are conducted safely with automatic rollback on failure and that the user is informed of the agent's confidence level.
Output: SafetyLoop orchestrator and Bayesian Scoring model.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/03-guided-repair-human-approved-fixes/03-RESEARCH.md
@agent-rs/src/agent_core/diagnostics/reasoning.rs
@agent-rs/src/agent_core/repair/snapshots.rs
@agent-rs/src/agent_core/repair/tools.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Bayesian Confidence Scoring & Update Mod</name>
  <files>agent-rs/src/agent_core/repair/scoring.rs, agent-rs/src/agent_core/repair/mod.rs, agent-rs/src/agent_core/diagnostics/reasoning.rs</files>
  <action>
    - In `agent-rs/src/agent_core/repair/mod.rs`, add `pub mod scoring;`.
    - Create `agent-rs/src/agent_core/repair/scoring.rs`.
    - Implement `calculate_confidence` based on the Bayesian Scaffold: `(Token Probs * Tool Reliability) - Information Gap`.
    - Update `Hypothesis` struct in `agent-rs/src/agent_core/diagnostics/reasoning.rs` to include a `confidence: f64` field.
    - Implement a method in `DiagnosticEngine` to update hypothesis confidence based on collected evidence coverage.
  </action>
  <verify>
    <automated>cargo test agent_core::repair::scoring</automated>
  </verify>
  <done>Agent can quantify the certainty of its diagnoses using a Bayesian model. Scoring module is declared.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Transactional Safety Loop & Warning Logic</name>
  <files>agent-rs/src/agent_core/repair/workflow.rs, agent-rs/src/agent_core/repair/mod.rs, agent-rs/src/agent_core/tool_runtime.rs</files>
  <action>
    - In `agent-rs/src/agent_core/repair/mod.rs`, add `pub mod workflow;`.
    - Create `agent-rs/src/agent_core/repair/workflow.rs`.
    - Implement `SafetyLoop` runner orchestrating Pre-flight -> Snapshot -> Execution -> Validation -> Rollback.
    - **FIX-03 Threshold Logic:** In the `SafetyLoop`, before requesting permission, check if the hypothesis confidence is `< 0.8`.
    - If `< 0.8`, append a prominent "LOW CONFIDENCE WARNING" to the `PermissionRequest` description.
    - Integrate `SafetyLoop` into `ToolRuntime` for tools flagged as requiring transactional safety.
  </action>
  <verify>
    <automated>cargo test agent_core::repair::workflow::test_low_confidence_warning_logic</automated>
  </verify>
  <done>Repairs are wrapped in a safety loop with automatic rollback and conditional low-confidence warnings. Workflow module is declared.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Auto-Rollback Verification Tests</name>
  <files>agent-rs/tests/repair_safety_validation.rs</files>
  <action>
    - Create a new integration test file `agent-rs/tests/repair_safety_validation.rs`.
    - Mock a failing repair that passes execution but fails validation.
    - Assert that `restore_snapshot` was called automatically by the `SafetyLoop`.
    - Assert that the user receives a "Repair failed, system rolled back" status.
  </action>
  <verify>
    <automated>cargo test --test repair_safety_validation</automated>
  </verify>
  <done>Auto-rollback mechanism is verified and robust against repair failures.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| SafetyLoop -> SnapshotManager | Safety loop depends on the integrity of the snapshot for recovery. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Denial of Service | SafetyLoop | mitigate | Implement a timeout for the "Validation" step to prevent the safety loop from hanging in a transition state. |
| T-03-03 | Tampering | Scoring | mitigate | Ensure the confidence score cannot be manipulated by the model (e.g., via prompt injection) by calculating it in the Rust core using objective evidence metrics. |
</threat_model>

<verification>
Verify that the `SafetyLoop` correctly handles both successful repairs and failures requiring rollback.
</verification>

<success_criteria>
- Bayesian confidence scoring is active and visible in hypotheses.
- Transactional Safety Loop is implemented with Pre-flight, Snapshot, and Validation steps.
- Automatic rollback is verified by integration tests.
- Low-confidence warnings are triggered correctly for confidence < 80%.
</success_criteria>

<output>
After completion, create `.planning/phases/03-guided-repair-human-approved-fixes/03-03-SUMMARY.md`
</output>
