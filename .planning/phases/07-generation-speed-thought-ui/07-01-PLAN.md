---
wave: 1
depends_on: []
files_modified:
  - agent-rs/src/main.rs
  - agent-rs/src/server.rs
  - web-ui/src/App.tsx
  - web-ui/src/index.css
  - scripts/config.py
autonomous: true
---

# Plan 07-01: Expose Agent Reasoning & TTFT

## Objective
Tune backend parameters to maximize generation speed and properly surface the local LLM's internal `<think>` reasoning traces into beautiful frontend UI `<thinking>` panels.

## Requirements Addressed
- PERF-02: The inference pipeline must prioritize fast TTFT via configuration tuning.
- UX-01: The orchestrator must extract internal `<think>` blocks and expose them visibly in the terminal and web UI.

## Tasks

<task>
<read_first>
- agent-rs/src/main.rs
- agent-rs/src/server.rs
</read_first>
<action>
Modify `agent-rs/src/main.rs` to rename `strip_think_blocks` to `expose_think_blocks`. Change its internal replacement logic so that `<think>` is mapped to `\n<thinking>\n`. Update the `crate::strip_think_blocks` usages in `server.rs` ensuring SSE handles the new tag properly.
</action>
<acceptance_criteria>
`agent-rs/src/main.rs` contains the `expose_think_blocks` function mapping `<think>` blocks to `<thinking>` elements.
</acceptance_criteria>
</task>

<task>
<read_first>
- web-ui/package.json
- web-ui/src/App.tsx
- web-ui/src/index.css
</read_first>
<action>
Install the `rehype-raw` package for React Markdown. In `App.tsx`, inject `rehypeRaw` into the `rehypePlugins` array for the `ReactMarkdown` component. Finally, append a `.thinking` or `thinking` element tag block in `index.css` with a visually separate Tailwind color pattern to mute the reasoning text.
</action>
<acceptance_criteria>
`web-ui/src/App.tsx` imports `rehype-raw` and the `<thinking>` HTML tag is styled in `index.css`.
</acceptance_criteria>
</task>

<task>
<read_first>
- scripts/config.py
</read_first>
<action>
Append the `--flashattention` flag to `KOBOLDCPP_ARGS` inside `scripts/config.py` to dramatically improve CUDA and Vulkan TTFT. 
</action>
<acceptance_criteria>
`scripts/config.py` contains the `--flashattention` flag inside `KOBOLDCPP_ARGS`.
</acceptance_criteria>
</task>
