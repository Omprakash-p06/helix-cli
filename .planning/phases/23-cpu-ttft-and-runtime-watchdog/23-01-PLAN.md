---
phase: 23-cpu-ttft-and-runtime-watchdog
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - agent-rs/src/runtime_profile.rs
  - agent-rs/src/watchdog.rs
  - agent-rs/src/main.rs
  - scripts/start_server.py
  - scripts/config.py
  - agent-rs/tests/runtime_profile_watchdog.rs
  - tests/test_start_server_runtime_profile.py
autonomous: true
requirements:
  - PERF-04
  - PERF-05
must_haves:
  truths:
    - "On modest CPU systems, startup applies a deterministic low-latency runtime profile that reduces first-token delay."
    - "When health probes fail repeatedly, runtime enters controlled recovery with bounded restart attempts and cooldown."
    - "Recovery actions are visible through explicit status messages and do not spin in infinite retry loops."
    - "Runtime profile and watchdog behavior are covered by automated tests."
  artifacts:
    - path: "agent-rs/src/runtime_profile.rs"
      provides: "Typed runtime profile selection and TTFT guardrail thresholds"
      min_lines: 120
    - path: "agent-rs/src/watchdog.rs"
      provides: "Watchdog state machine, restart budget, and backoff/cooldown policy"
      min_lines: 140
    - path: "agent-rs/src/main.rs"
      provides: "Integration of profile selection and watchdog-driven recovery in request path"
      min_lines: 2200
    - path: "scripts/start_server.py"
      provides: "Profile-aware runtime overrides applied to launcher command arguments"
      min_lines: 240
    - path: "scripts/config.py"
      provides: "Persisted runtime profile defaults for startup behavior"
      min_lines: 40
    - path: "agent-rs/tests/runtime_profile_watchdog.rs"
      provides: "Regression tests for profile downgrade, budget enforcement, and state transitions"
      min_lines: 100
    - path: "tests/test_start_server_runtime_profile.py"
      provides: "Launcher override tests for profile-driven batch/context/thread behavior"
      min_lines: 70
  key_links:
    - from: "agent-rs/src/main.rs"
      to: "agent-rs/src/runtime_profile.rs"
      via: "startup profile selection and TTFT threshold checks"
      pattern: "select_runtime_profile|ttft_target_ms"
    - from: "agent-rs/src/main.rs"
      to: "agent-rs/src/watchdog.rs"
      via: "health-failure transitions and restart budget decisions"
      pattern: "WatchdogState|can_restart|next_backoff"
    - from: "agent-rs/src/main.rs"
      to: "scripts/start_server.py"
      via: "env override handoff for backend/layer/batch/profile"
      pattern: "HELIX_BACKEND_HINT|HELIX_GPU_LAYERS|HELIX_RUNTIME_PROFILE"
    - from: "scripts/start_server.py"
      to: "scripts/config.py"
      via: "runtime default values used when env overrides are absent"
      pattern: "BATCH_SIZE|UBATCH_SIZE|CPU_THREADS|CONTEXT_SIZE"
---

<objective>
Implement CPU-focused runtime profile selection and a controlled watchdog recovery loop so first-token latency improves on modest hardware and long-running inference remains stable without endless restart churn.

Purpose: Satisfy PERF-04 and PERF-05 by turning current ad hoc retry/recovery primitives into explicit profile and state-machine behavior.
Output: Runtime profile contracts, watchdog policy integration, launcher/config bridging, and automated regression tests.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/23-cpu-ttft-and-runtime-watchdog/23-RESEARCH.md
@agent-rs/src/main.rs
@scripts/start_server.py
@scripts/system_check.py
@setup.py

<interfaces>
From agent-rs/src/main.rs:
```rust
async fn is_model_server_reachable(client: &Client, base_url: &str) -> bool;
async fn probe_model_chat_ready(client: &Client, app_config: &config::AppConfig) -> bool;
async fn maybe_boot_model_server(client: &Client, app_config: &config::AppConfig) -> bool;
async fn send_with_recovery(
    client: &Client,
    url: &str,
    request_body: &Value,
    app_config: &config::AppConfig,
) -> Result<reqwest::Response, reqwest::Error>;
```

From scripts/start_server.py:
```python
def apply_runtime_overrides():
def run_llama_server(model_path):
def run_koboldcpp(model_path):
```

From scripts/system_check.py:
```python
def detect_specs():
def get_recommended_config(tier, gpu, igpu, backend_hint, ram=None):
```

Runtime constraint: recovery already delegates process launch to Python (`scripts/start_server.py`) while orchestration decisions live in Rust (`main.rs`). Keep this split.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add runtime profile and watchdog contracts with testable state logic</name>
  <files>agent-rs/src/runtime_profile.rs, agent-rs/src/watchdog.rs, agent-rs/tests/runtime_profile_watchdog.rs</files>
  <behavior>
    - Define typed runtime profile contracts for `latency_cpu`, `balanced_cpu`, and `safe_recovery` with explicit `ttft_target_ms`, batch/context/thread settings (PERF-04).
    - Define watchdog states (`Healthy`, `Degraded`, `Recovering`, `Cooldown`, `Unhealthy`) and transition helpers, restart budget checks, and backoff progression (PERF-05).
    - Add focused tests for profile downgrade thresholds and watchdog transition/budget behavior.
  </behavior>
  <action>Create standalone Rust modules for profile selection and watchdog policy so `main.rs` integration remains thin and deterministic. Keep logic pure and unit-testable; avoid embedding state-machine rules directly inside request loops.</action>
  <verify>
    <automated>cd agent-rs && cargo test -q --test runtime_profile_watchdog</automated>
  </verify>
  <done>Profile and watchdog contracts exist with passing regression tests for TTFT guardrail and restart policy behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integrate profile selection and bounded watchdog recovery into runtime path</name>
  <files>agent-rs/src/main.rs, scripts/start_server.py, scripts/config.py</files>
  <behavior>
    - Apply runtime profile selection at startup and feed profile values into server launch env overrides for CPU-first latency behavior (PERF-04).
    - Replace unbounded recovery tendencies with watchdog-governed restart budget and cooldown checks before auto-restarts (PERF-05).
    - Emit explicit status messages for profile choice, watchdog state transitions, and cooldown lockouts.
  </behavior>
  <action>Integrate the new profile/watchdog modules into `send_with_recovery` and server boot paths. Preserve existing health probes (`/v1/models` + chat probe), but gate restart attempts through watchdog policy helpers. Extend Python override handling only as needed to honor profile-driven env inputs without creating a second launcher path.</action>
  <verify>
    <automated>cd agent-rs && cargo test -q</automated>
  </verify>
  <done>Runtime uses explicit profile defaults and watchdog-governed recovery with visible transitions and bounded restart behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add cross-layer validation for launcher profile overrides and cooldown safety</name>
  <files>tests/test_start_server_runtime_profile.py, agent-rs/tests/runtime_profile_watchdog.rs, scripts/start_server.py</files>
  <behavior>
    - Add Python tests verifying profile/env override precedence for batch, ubatch, context, threads, backend, and GPU layer behavior.
    - Extend Rust watchdog tests to cover cooldown lockout and subsequent recovery eligibility windows.
    - Ensure tests validate no infinite retry sequence when restart budget is exceeded.
  </behavior>
  <action>Implement automated assertions that profile selection affects launcher arguments as intended and watchdog lockout semantics prevent repeated uncontrolled restart attempts. Keep tests deterministic by avoiding real server boots; test pure configuration and state logic.</action>
  <verify>
    <automated>pytest -q tests/test_start_server_runtime_profile.py && cd agent-rs && cargo test -q --test runtime_profile_watchdog</automated>
  </verify>
  <done>Automated tests prove profile override behavior and watchdog cooldown policy across Python/Rust boundaries.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runtime request path -> watchdog policy | Untrusted network/runtime failures influence restart decisions here |
| Rust orchestrator -> Python launcher env | Recovery parameters cross process boundary via environment variables |
| launcher overrides -> model server process | Profile-selected parameters alter backend and resource usage at process startup |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01 | D | recovery loop | mitigate | Enforce watchdog restart budget and cooldown lockout before invoking auto-restart logic. |
| T-23-02 | T | env override inputs | mitigate | Normalize and bound runtime profile-derived env values (threads, batch, context, gpu layers) before passing to launcher. |
| T-23-03 | R | recovery observability | mitigate | Emit explicit status messages with state/reason codes on profile selection, restart attempts, and cooldown transitions. |
| T-23-04 | E | backend fallback transitions | accept | Existing launcher fallback path retained; risk accepted for this phase with bounded restarts and visible lockout messaging. |
</threat_model>

<verification>
- `cd agent-rs && cargo test -q --test runtime_profile_watchdog` passes.
- `cd agent-rs && cargo test -q` passes.
- `pytest -q tests/test_start_server_runtime_profile.py` passes.
- Manual smoke: induce server-down condition and confirm watchdog transitions to recovery then cooldown instead of infinite retries.
</verification>

<success_criteria>
- PERF-04: Runtime applies deterministic CPU-aware profile settings that prioritize TTFT on modest hardware.
- PERF-05: Health failures trigger bounded, stateful recovery with cooldown lockout and clear diagnostics.
- Recovery path remains single-entry and does not duplicate launcher orchestration layers.
</success_criteria>

<output>
After completion, create `.planning/phases/23-cpu-ttft-and-runtime-watchdog/23-01-SUMMARY.md`
</output>
