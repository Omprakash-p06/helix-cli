---
phase: 20-security-execution-guardrails
plan: 03
type: execute
wave: 3
depends_on:
  - 20-01
  - 20-02
files_modified:
  - agent-rs/src/main.rs
  - agent-rs/src/server.rs
  - agent-rs/src/tui.rs
  - agent-rs/tests/security_guardrails.rs
autonomous: true
requirements:
  - SEC-01
  - SEC-02
  - SEC-03
must_haves:
  truths:
    - "Policy enforcement is applied consistently in terminal and web execution paths."
    - "Users receive clear refusal/approval-required feedback instead of cryptic failures."
    - "Automated tests verify no policy bypass across runtime modes."
  artifacts:
    - path: "agent-rs/src/main.rs"
      provides: "terminal/tui policy integration before tool dispatch"
      contains: "PolicyDecision"
    - path: "agent-rs/src/server.rs"
      provides: "web mode policy integration before tool dispatch"
      contains: "policy decision handling"
    - path: "agent-rs/tests/security_guardrails.rs"
      provides: "cross-path regression coverage"
      exports: ["terminal_path_denies_unsafe", "web_path_denies_unsafe"]
  key_links:
    - from: "agent-rs/src/main.rs"
      to: "agent-rs/src/security/policy.rs"
      via: "tool-call preflight"
      pattern: "evaluate_tool_call"
    - from: "agent-rs/src/server.rs"
      to: "agent-rs/src/security/policy.rs"
      via: "pre-dispatch gate"
      pattern: "PolicyDecision"
---

<objective>
Wire policy enforcement into all runtime tool dispatch flows and add regression tests proving no bypass.

Purpose: Complete SEC-01/02/03 by ensuring consistent behavior across TUI/terminal/web modes.
Output: Integrated enforcement, user-visible feedback, and cross-path tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/20-security-execution-guardrails/20-RESEARCH.md
@.planning/phases/20-security-execution-guardrails/20-01-PLAN.md
@.planning/phases/20-security-execution-guardrails/20-02-PLAN.md
@agent-rs/src/main.rs
@agent-rs/src/server.rs
@agent-rs/src/tui.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Integrate policy gate in terminal and TUI tool dispatch loop</name>
  <files>agent-rs/src/main.rs, agent-rs/src/tui.rs</files>
  <behavior>
    - Test 1: Denied tool calls are surfaced as structured tool failures.
    - Test 2: Approval-required calls produce explicit status feedback.
    - Test 3: Allowed calls preserve current execution flow.
  </behavior>
  <action>Before executing any model-requested tool call, evaluate with policy context and branch on `PolicyDecision`. Keep event ordering (`ToolStart`/`ToolResult`) coherent while preventing execution for denied requests.</action>
  <verify>
    <automated>cd agent-rs && cargo test security_guardrails::terminal -- --nocapture</automated>
  </verify>
  <done>Terminal/TUI path enforces policy without regressions in tool status UX.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integrate policy gate in web server tool dispatch and add cross-path tests</name>
  <files>agent-rs/src/server.rs, agent-rs/tests/security_guardrails.rs</files>
  <behavior>
    - Test 1: Web path denies unsafe tool calls before execution.
    - Test 2: Web path and terminal path produce consistent deny reason codes.
    - Test 3: Read-only tier blocks mutation tools in both paths.
  </behavior>
  <action>Apply the same pre-dispatch policy evaluation in web mode tool handling and add integration-style tests that assert parity across terminal and web execution paths.</action>
  <verify>
    <automated>cd agent-rs && cargo test security_guardrails -- --nocapture</automated>
  </verify>
  <done>Policy behavior is consistent across runtime modes with automated parity coverage.</done>
</task>

<task type="auto">
  <name>Task 3: End-to-end verification and quality gate</name>
  <files>agent-rs/src/main.rs, agent-rs/src/server.rs, agent-rs/tests/security_guardrails.rs</files>
  <action>Run focused and full test suites, then clippy/format checks required by existing project quality constraints. Confirm there is no execution-path bypass and failure messages remain actionable.</action>
  <verify>
    <automated>cd agent-rs && cargo test && cargo clippy -- -D warnings</automated>
  </verify>
  <done>All tests and lint checks pass; SEC-01/02/03 are enforceable and verifiable in both interactive and web modes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| llm-tool-plan→runtime-dispatch | Untrusted model-generated actions enter orchestrator execution paths |
| runtime-policy→user-feedback | Security denial decisions must be surfaced without leaking sensitive internals |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-07 | E | terminal dispatch loop | mitigate | Enforce `PolicyDecision` gate before any tool execution in `main.rs`. |
| T-20-08 | E | web dispatch loop | mitigate | Mirror the same policy gate and denial branching in `server.rs`. |
| T-20-09 | R | inconsistent path behavior | mitigate | Add cross-path tests ensuring identical reason codes and blocking behavior. |
</threat_model>

<verification>
Execute targeted guardrail tests and full quality gates (`test`, `clippy`) to confirm no bypass or regressions.
</verification>

<success_criteria>
- All execution paths enforce the same policy decisions.
- Denied/approval-required outcomes are user-visible and structured.
- Automated tests prove cross-path parity and no bypass.
</success_criteria>

<output>
After completion, create `.planning/phases/20-security-execution-guardrails/20-03-SUMMARY.md`
</output>
---
phase: 20-security-execution-guardrails
plan: 03
type: execute
wave: 3
depends_on:
  - 20-01
  - 20-02
files_modified:
  - agent-rs/src/main.rs
  - agent-rs/src/server.rs
  - agent-rs/src/tui.rs
  - agent-rs/tests/security_guardrails.rs
autonomous: true
requirements:
  - SEC-01
  - SEC-02
  - SEC-03
must_haves:
  truths:
    - "Policy enforcement is applied consistently in terminal and web execution paths."
    - "Users receive clear refusal/approval-required feedback instead of cryptic failures."
    - "Automated tests verify no policy bypass across runtime modes."
  artifacts:
    - path: "agent-rs/src/main.rs"
      provides: "terminal/tui policy integration before tool dispatch"
      contains: "PolicyDecision"
    - path: "agent-rs/src/server.rs"
      provides: "web mode policy integration before tool dispatch"
      contains: "policy decision handling"
    - path: "agent-rs/tests/security_guardrails.rs"
      provides: "cross-path regression coverage"
      exports: ["terminal_path_denies_unsafe", "web_path_denies_unsafe"]
  key_links:
    - from: "agent-rs/src/main.rs"
      to: "agent-rs/src/security/policy.rs"
      via: "tool-call preflight"
      pattern: "evaluate_tool_call"
    - from: "agent-rs/src/server.rs"
      to: "agent-rs/src/security/policy.rs"
      via: "pre-dispatch gate"
      pattern: "PolicyDecision"
---

<objective>
Wire policy enforcement into all runtime tool dispatch flows and add regression tests proving no bypass.

Purpose: Complete SEC-01/02/03 by ensuring consistent behavior across TUI/terminal/web modes.
Output: Integrated enforcement, user-visible feedback, and cross-path tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/20-security-execution-guardrails/20-RESEARCH.md
@.planning/phases/20-security-execution-guardrails/20-01-PLAN.md
@.planning/phases/20-security-execution-guardrails/20-02-PLAN.md
@agent-rs/src/main.rs
@agent-rs/src/server.rs
@agent-rs/src/tui.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Integrate policy gate in terminal and TUI tool dispatch loop</name>
  <files>agent-rs/src/main.rs, agent-rs/src/tui.rs</files>
  <behavior>
    - Test 1: Denied tool calls are surfaced as structured tool failures.
    - Test 2: Approval-required calls produce explicit status feedback.
    - Test 3: Allowed calls preserve current execution flow.
  </behavior>
  <action>Before executing any model-requested tool call, evaluate with policy context and branch on `PolicyDecision`. Keep event ordering (`ToolStart`/`ToolResult`) coherent while preventing execution for denied requests.</action>
  <verify>
    <automated>cd agent-rs && cargo test security_guardrails::terminal -- --nocapture</automated>
  </verify>
  <done>Terminal/TUI path enforces policy without regressions in tool status UX.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integrate policy gate in web server tool dispatch and add cross-path tests</name>
  <files>agent-rs/src/server.rs, agent-rs/tests/security_guardrails.rs</files>
  <behavior>
    - Test 1: Web path denies unsafe tool calls before execution.
    - Test 2: Web path and terminal path produce consistent deny reason codes.
    - Test 3: Read-only tier blocks mutation tools in both paths.
  </behavior>
  <action>Apply the same pre-dispatch policy evaluation in web mode tool handling and add integration-style tests that assert parity across terminal and web execution paths.</action>
  <verify>
    <automated>cd agent-rs && cargo test security_guardrails -- --nocapture</automated>
  </verify>
  <done>Policy behavior is consistent across runtime modes with automated parity coverage.</done>
</task>

<task type="auto">
  <name>Task 3: End-to-end verification and quality gate</name>
  <files>agent-rs/src/main.rs, agent-rs/src/server.rs, agent-rs/tests/security_guardrails.rs</files>
  <action>Run focused and full test suites, then clippy/format checks required by existing project quality constraints. Confirm there is no execution-path bypass and failure messages remain actionable.</action>
  <verify>
    <automated>cd agent-rs && cargo test && cargo clippy -- -D warnings</automated>
  </verify>
  <done>All tests and lint checks pass; SEC-01/02/03 are enforceable and verifiable in both interactive and web modes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| llm-tool-plan→runtime-dispatch | Untrusted model-generated actions enter orchestrator execution paths |
| runtime-policy→user-feedback | Security denial decisions must be surfaced without leaking sensitive internals |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-07 | E | terminal dispatch loop | mitigate | Enforce `PolicyDecision` gate before any tool execution in `main.rs`. |
| T-20-08 | E | web dispatch loop | mitigate | Mirror the same policy gate and denial branching in `server.rs`. |
| T-20-09 | R | inconsistent path behavior | mitigate | Add cross-path tests ensuring identical reason codes and blocking behavior. |
</threat_model>

<verification>
Execute targeted guardrail tests and full quality gates (`test`, `clippy`) to confirm no bypass or regressions.
</verification>

<success_criteria>
- All execution paths enforce the same policy decisions.
- Denied/approval-required outcomes are user-visible and structured.
- Automated tests prove cross-path parity and no bypass.
</success_criteria>

<output>
After completion, create `.planning/phases/20-security-execution-guardrails/20-03-SUMMARY.md`
</output>
