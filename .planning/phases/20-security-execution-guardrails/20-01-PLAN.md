---
phase: 20-security-execution-guardrails
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - agent-rs/src/security/mod.rs
  - agent-rs/src/security/policy.rs
  - agent-rs/src/config.rs
  - scripts/config.py
autonomous: true
requirements:
  - SEC-01
must_haves:
  truths:
    - "Runtime supports explicit permission tiers for tool capabilities."
    - "Policy decisions are represented as typed outcomes (allow/approval/deny)."
    - "Rust runtime can read permission tier from generated Python config."
  artifacts:
    - path: "agent-rs/src/security/policy.rs"
      provides: "Security policy contracts and tier model"
      exports: ["PermissionTier", "PolicyDecision", "PolicyContext"]
    - path: "scripts/config.py"
      provides: "Configurable TOOL_PERMISSION_TIER"
      contains: "TOOL_PERMISSION_TIER"
  key_links:
    - from: "scripts/config.py"
      to: "agent-rs/src/config.rs"
      via: "Python bridge JSON payload"
      pattern: "tool_permission_tier|TOOL_PERMISSION_TIER"
    - from: "agent-rs/src/config.rs"
      to: "agent-rs/src/security/policy.rs"
      via: "typed enum mapping"
      pattern: "PermissionTier"
---

<objective>
Establish the security policy contract and permission-tier configuration that all tool execution paths will rely on.

Purpose: Create a deterministic foundation for SEC-01 before enforcement logic and integration wiring.
Output: New security module contracts and configuration bridge updates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-security-execution-guardrails/20-RESEARCH.md
@agent-rs/src/config.rs
@scripts/config.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define security policy contracts and permission tiers</name>
  <files>agent-rs/src/security/mod.rs, agent-rs/src/security/policy.rs</files>
  <action>Create a new `security` module with typed policy contracts: `PermissionTier` (`ReadOnly`, `WorkspaceWrite`, `FullExec`), `PolicyDecision` (`Allow`, `RequireApproval`, `Deny`), and `PolicyContext`. Add serde-friendly parsing helpers so policy decisions are deterministic and testable. Keep this task contract-only: no execution-path wiring yet.</action>
  <verify>
    <automated>cd agent-rs && cargo test security::policy -- --nocapture</automated>
  </verify>
  <done>Security contracts compile, unit tests cover enum parsing/default behavior, and module exports are stable for downstream tasks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add configuration bridge for tool permission tier</name>
  <files>scripts/config.py, agent-rs/src/config.rs</files>
  <behavior>
    - Test 1: Missing `TOOL_PERMISSION_TIER` defaults to safe tier.
    - Test 2: Valid values map to `PermissionTier` variants.
    - Test 3: Invalid value falls back to safe tier with warning.
  </behavior>
  <action>Add `TOOL_PERMISSION_TIER` in Python config generation/reading and extend Rust config load path to parse it into the new policy tier type. Preserve existing startup behavior and avoid breaking legacy config files.</action>
  <verify>
    <automated>cd agent-rs && cargo test config -- --nocapture</automated>
  </verify>
  <done>Runtime config exposes a typed permission tier with backward-compatible defaults and tests proving fallback behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| model-output→tool-dispatch | Untrusted tool call intent crosses into privileged local execution |
| config-file→runtime-policy | External config values influence enforcement behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | E | permission tier defaults | mitigate | Default to `ReadOnly`/safe tier on missing or invalid config values and test fallback paths. |
| T-20-02 | T | policy contract parsing | mitigate | Use strict enum mapping and reject unknown tier strings with deterministic fallback. |
| T-20-03 | R | policy decision consistency | mitigate | Centralize decision types in one module and enforce typed outcomes instead of ad-hoc strings. |
</threat_model>

<verification>
Run unit tests for security policy and config parsing. Confirm no behavior regression in startup config loading.
</verification>

<success_criteria>
- Security module exists with reusable policy contracts.
- Permission tier is configurable and safely defaulted.
- Downstream plans can consume typed policy context without interface changes.
</success_criteria>

<output>
After completion, create `.planning/phases/20-security-execution-guardrails/20-01-SUMMARY.md`
</output>
---
phase: 20-security-execution-guardrails
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - agent-rs/src/security/mod.rs
  - agent-rs/src/security/policy.rs
  - agent-rs/src/config.rs
  - scripts/config.py
autonomous: true
requirements:
  - SEC-01
must_haves:
  truths:
    - "Runtime supports explicit permission tiers for tool capabilities."
    - "Policy decisions are represented as typed outcomes (allow/approval/deny)."
    - "Rust runtime can read permission tier from generated Python config."
  artifacts:
    - path: "agent-rs/src/security/policy.rs"
      provides: "Security policy contracts and tier model"
      exports: ["PermissionTier", "PolicyDecision", "PolicyContext"]
    - path: "scripts/config.py"
      provides: "Configurable TOOL_PERMISSION_TIER"
      contains: "TOOL_PERMISSION_TIER"
  key_links:
    - from: "scripts/config.py"
      to: "agent-rs/src/config.rs"
      via: "Python bridge JSON payload"
      pattern: "tool_permission_tier|TOOL_PERMISSION_TIER"
    - from: "agent-rs/src/config.rs"
      to: "agent-rs/src/security/policy.rs"
      via: "typed enum mapping"
      pattern: "PermissionTier"
---

<objective>
Establish the security policy contract and permission-tier configuration that all tool execution paths will rely on.

Purpose: Create a deterministic foundation for SEC-01 before enforcement logic and integration wiring.
Output: New security module contracts and configuration bridge updates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-security-execution-guardrails/20-RESEARCH.md
@agent-rs/src/config.rs
@scripts/config.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define security policy contracts and permission tiers</name>
  <files>agent-rs/src/security/mod.rs, agent-rs/src/security/policy.rs</files>
  <action>Create a new `security` module with typed policy contracts: `PermissionTier` (`ReadOnly`, `WorkspaceWrite`, `FullExec`), `PolicyDecision` (`Allow`, `RequireApproval`, `Deny`), and `PolicyContext`. Add serde-friendly parsing helpers so policy decisions are deterministic and testable. Keep this task contract-only: no execution-path wiring yet.</action>
  <verify>
    <automated>cd agent-rs && cargo test security::policy -- --nocapture</automated>
  </verify>
  <done>Security contracts compile, unit tests cover enum parsing/default behavior, and module exports are stable for downstream tasks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add configuration bridge for tool permission tier</name>
  <files>scripts/config.py, agent-rs/src/config.rs</files>
  <behavior>
    - Test 1: Missing `TOOL_PERMISSION_TIER` defaults to safe tier.
    - Test 2: Valid values map to `PermissionTier` variants.
    - Test 3: Invalid value falls back to safe tier with warning.
  </behavior>
  <action>Add `TOOL_PERMISSION_TIER` in Python config generation/reading and extend Rust config load path to parse it into the new policy tier type. Preserve existing startup behavior and avoid breaking legacy config files.</action>
  <verify>
    <automated>cd agent-rs && cargo test config -- --nocapture</automated>
  </verify>
  <done>Runtime config exposes a typed permission tier with backward-compatible defaults and tests proving fallback behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| model-output→tool-dispatch | Untrusted tool call intent crosses into privileged local execution |
| config-file→runtime-policy | External config values influence enforcement behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | E | permission tier defaults | mitigate | Default to `ReadOnly`/safe tier on missing or invalid config values and test fallback paths. |
| T-20-02 | T | policy contract parsing | mitigate | Use strict enum mapping and reject unknown tier strings with deterministic fallback. |
| T-20-03 | R | policy decision consistency | mitigate | Centralize decision types in one module and enforce typed outcomes instead of ad-hoc strings. |
</threat_model>

<verification>
Run unit tests for security policy and config parsing. Confirm no behavior regression in startup config loading.
</verification>

<success_criteria>
- Security module exists with reusable policy contracts.
- Permission tier is configurable and safely defaulted.
- Downstream plans can consume typed policy context without interface changes.
</success_criteria>

<output>
After completion, create `.planning/phases/20-security-execution-guardrails/20-01-SUMMARY.md`
</output>
