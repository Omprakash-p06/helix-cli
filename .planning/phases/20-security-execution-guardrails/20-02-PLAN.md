---
phase: 20-security-execution-guardrails
plan: 02
type: execute
wave: 2
depends_on:
  - 20-01
files_modified:
  - agent-rs/Cargo.toml
  - agent-rs/src/security/policy.rs
  - agent-rs/src/tools.rs
autonomous: true
requirements:
  - SEC-02
  - SEC-03
must_haves:
  truths:
    - "Terminal command requests are parsed and validated before execution."
    - "Prompt-injection and exfiltration patterns are denied before dispatch."
    - "Denied tool calls return structured refusal reasons."
  artifacts:
    - path: "agent-rs/src/security/policy.rs"
      provides: "command risk scoring and injection heuristics"
      exports: ["evaluate_tool_call", "evaluate_command_risk"]
    - path: "agent-rs/src/tools.rs"
      provides: "policy-aware command execution entry"
      contains: "PolicyDecision"
  key_links:
    - from: "agent-rs/src/security/policy.rs"
      to: "agent-rs/src/tools.rs"
      via: "pre-execution policy evaluation"
      pattern: "evaluate_tool_call|PolicyDecision"
    - from: "agent-rs/src/tools.rs"
      to: "shell execution"
      via: "allow-only execution path"
      pattern: "Command::new\("sh"\)|Command::new\("cmd"\)"
---

<objective>
Implement strict tool argument validation and prompt-injection refusal logic for command execution.

Purpose: Close SEC-02 and SEC-03 by making unsafe tool calls fail before any side effects occur.
Output: Policy evaluators, command-risk scoring, and structured denial behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/20-security-execution-guardrails/20-RESEARCH.md
@.planning/phases/20-security-execution-guardrails/20-01-PLAN.md
@agent-rs/src/security/policy.rs
@agent-rs/src/tools.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement command parsing and risk scoring policy</name>
  <files>agent-rs/Cargo.toml, agent-rs/src/security/policy.rs</files>
  <behavior>
    - Test 1: Safe commands score as `Allow`.
    - Test 2: Disallowed operators/destructive patterns score as `Deny`.
    - Test 3: Medium-risk mutations score as `RequireApproval`.
  </behavior>
  <action>Add parser-backed command token validation (using selected parsing crates where needed) and a rule set for dangerous operators, destructive commands, and exfiltration signatures. Return typed `PolicyDecision` values with stable reason codes and user-safe remediation text.</action>
  <verify>
    <automated>cd agent-rs && cargo test security::policy::risk -- --nocapture</automated>
  </verify>
  <done>Risk evaluation is deterministic and covered by unit tests for allow/approval/deny pathways.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce policy in run_terminal_command preflight</name>
  <files>agent-rs/src/tools.rs, agent-rs/src/security/policy.rs</files>
  <behavior>
    - Test 1: Denied command never reaches shell spawn.
    - Test 2: Approval-required command returns explicit approval-required result.
    - Test 3: Allowed command continues to existing execution path.
  </behavior>
  <action>Insert a pre-execution gate for `run_terminal_command` that calls `evaluate_tool_call` and blocks/labels unsafe calls before spawning shell processes. Keep existing sandbox behavior for file tools unchanged.</action>
  <verify>
    <automated>cd agent-rs && cargo test tools::command_policy -- --nocapture</automated>
  </verify>
  <done>Command tool execution is policy-gated with structured refusal outcomes and no bypass in direct tool path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| model-tool-args→command-parser | Untrusted arguments are interpreted for local execution |
| policy-decision→shell-spawn | Decision gate controls side-effectful process spawning |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-04 | T | command argument parsing | mitigate | Parse and validate command tokens before execution; deny unsupported operators. |
| T-20-05 | I | prompt-injection/exfiltration payloads | mitigate | Add deterministic deny rules for override/exfiltration signatures in args. |
| T-20-06 | E | shell spawn path | mitigate | Require explicit `Allow` policy decision before any shell process spawn. |
</threat_model>

<verification>
Run targeted policy and tool tests plus full crate tests to ensure no regressions.
</verification>

<success_criteria>
- Unsafe command patterns are blocked before execution.
- Prompt-injection-like payloads are denied deterministically.
- Structured refusal reasons are emitted for downstream UI/logging.
</success_criteria>

<output>
After completion, create `.planning/phases/20-security-execution-guardrails/20-02-SUMMARY.md`
</output>
---
phase: 20-security-execution-guardrails
plan: 02
type: execute
wave: 2
depends_on:
  - 20-01
files_modified:
  - agent-rs/Cargo.toml
  - agent-rs/src/security/policy.rs
  - agent-rs/src/tools.rs
autonomous: true
requirements:
  - SEC-02
  - SEC-03
must_haves:
  truths:
    - "Terminal command requests are parsed and validated before execution."
    - "Prompt-injection and exfiltration patterns are denied before dispatch."
    - "Denied tool calls return structured refusal reasons."
  artifacts:
    - path: "agent-rs/src/security/policy.rs"
      provides: "command risk scoring and injection heuristics"
      exports: ["evaluate_tool_call", "evaluate_command_risk"]
    - path: "agent-rs/src/tools.rs"
      provides: "policy-aware command execution entry"
      contains: "PolicyDecision"
  key_links:
    - from: "agent-rs/src/security/policy.rs"
      to: "agent-rs/src/tools.rs"
      via: "pre-execution policy evaluation"
      pattern: "evaluate_tool_call|PolicyDecision"
    - from: "agent-rs/src/tools.rs"
      to: "shell execution"
      via: "allow-only execution path"
      pattern: "Command::new\(\"sh\"\)|Command::new\(\"cmd\"\)"
---

<objective>
Implement strict tool argument validation and prompt-injection refusal logic for command execution.

Purpose: Close SEC-02 and SEC-03 by making unsafe tool calls fail before any side effects occur.
Output: Policy evaluators, command-risk scoring, and structured denial behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/20-security-execution-guardrails/20-RESEARCH.md
@.planning/phases/20-security-execution-guardrails/20-01-PLAN.md
@agent-rs/src/security/policy.rs
@agent-rs/src/tools.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement command parsing and risk scoring policy</name>
  <files>agent-rs/Cargo.toml, agent-rs/src/security/policy.rs</files>
  <behavior>
    - Test 1: Safe commands score as `Allow`.
    - Test 2: Disallowed operators/destructive patterns score as `Deny`.
    - Test 3: Medium-risk mutations score as `RequireApproval`.
  </behavior>
  <action>Add parser-backed command token validation (using selected parsing crates where needed) and a rule set for dangerous operators, destructive commands, and exfiltration signatures. Return typed `PolicyDecision` values with stable reason codes and user-safe remediation text.</action>
  <verify>
    <automated>cd agent-rs && cargo test security::policy::risk -- --nocapture</automated>
  </verify>
  <done>Risk evaluation is deterministic and covered by unit tests for allow/approval/deny pathways.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce policy in run_terminal_command preflight</name>
  <files>agent-rs/src/tools.rs, agent-rs/src/security/policy.rs</files>
  <behavior>
    - Test 1: Denied command never reaches shell spawn.
    - Test 2: Approval-required command returns explicit approval-required result.
    - Test 3: Allowed command continues to existing execution path.
  </behavior>
  <action>Insert a pre-execution gate for `run_terminal_command` that calls `evaluate_tool_call` and blocks/labels unsafe calls before spawning shell processes. Keep existing sandbox behavior for file tools unchanged.</action>
  <verify>
    <automated>cd agent-rs && cargo test tools::command_policy -- --nocapture</automated>
  </verify>
  <done>Command tool execution is policy-gated with structured refusal outcomes and no bypass in direct tool path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| model-tool-args→command-parser | Untrusted arguments are interpreted for local execution |
| policy-decision→shell-spawn | Decision gate controls side-effectful process spawning |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-04 | T | command argument parsing | mitigate | Parse and validate command tokens before execution; deny unsupported operators. |
| T-20-05 | I | prompt-injection/exfiltration payloads | mitigate | Add deterministic deny rules for override/exfiltration signatures in args. |
| T-20-06 | E | shell spawn path | mitigate | Require explicit `Allow` policy decision before any shell process spawn. |
</threat_model>

<verification>
Run targeted policy and tool tests plus full crate tests to ensure no regressions.
</verification>

<success_criteria>
- Unsafe command patterns are blocked before execution.
- Prompt-injection-like payloads are denied deterministically.
- Structured refusal reasons are emitted for downstream UI/logging.
</success_criteria>

<output>
After completion, create `.planning/phases/20-security-execution-guardrails/20-02-SUMMARY.md`
</output>
