---
wave: 1
depends_on: []
files_modified: ["agent-rs/src/tui.rs", "agent-rs/src/main.rs", "agent-rs/Cargo.toml"]
autonomous: false
---

<objective>
Wire live LLM token stream from agent-rs main to tui.rs with 30ms interval batching, extend ChatEntry for inline thinking spans, and inject chronologically styled tool execution states.
</objective>

<task>
<description>Refactor TuiEvent, ChatEntry, and ratatui UI rendering logic</description>
<read_first>
- agent-rs/src/tui.rs
- .planning/phases/11-output-polish-and-streaming/11-CONTEXT.md
</read_first>
<action>
1. Modify `TuiEvent` enum in `tui.rs`:
   - Replace `Token(String)` with `TokenChunk(String)`.
   - Add `ToolStart { name: String, args: String }`.
   - Replace `ToolResult { ... }` to match the exact `AppConfig` ToolResult output if it was previously different.
2. Modify `ChatEntry` in `tui.rs` to maintain inline styles:
   - Change struct to `pub struct ChatEntry { pub role: String, pub content: String, pub spans: Vec<(String, ratatui::style::Style)> }`
3. Update `TuiApp` event handling for `TokenChunk`:
   - As tokens append to `TuiApp::streaming_content`, parse for `<think>` boundaries and dynamically assign `Style::default().add_modifier(Modifier::DIM)` to text inside the tags.
4. Implement `Ctrl+T` toggle state `show_thoughts: bool` in `TuiApp` and `handle_key_event`.
5. Update `draw_chat_area` to iterate over `ChatEntry.spans` instead of plain `.content`, ignoring dimmed spans if `show_thoughts == false`.
6. Inject a styled block for `TuiEvent::ToolStart` into `chat_history`.
</action>
<acceptance_criteria>
- `agent-rs/src/tui.rs` contains `<think>` tracking logic in `ChatEntry`
- `agent-rs/src/tui.rs` handles `TuiEvent::ToolStart`
- `cargo check` in `agent-rs` compiles the updated structures
</acceptance_criteria>
</task>

<task>
<description>Wire tokio async event loop and batch interval into main.rs</description>
<read_first>
- agent-rs/src/main.rs
- agent-rs/src/tui.rs
</read_first>
<action>
1. In `agent-rs/src/main.rs`, replace the blocking `rustyline` loop with a `tokio::select!` based async event loop.
2. Start the TUI UI thread by calling `let (mut tui_rx, tui_tx) = tui::run_tui();`.
3. Wrap the main LLM streaming logic `stream.next()` into the `tokio::select!` block.
4. Add a `let mut flush_interval = tokio::time::interval(Duration::from_millis(30));` inside the streaming loop. Accumulate the HTTP chunk strings into a `buffer`, and on ticking, send `tui_tx.send(TuiEvent::TokenChunk(buffer.clone()))`.
5. When `main.rs` intercepts a tool block in `messages.push(ChatMessage { role: "tool"... })`, send `tui_tx.send(TuiEvent::ToolStart(...))` followed eventually by `TuiEvent::ToolResult`.
</action>
<acceptance_criteria>
- `agent-rs/src/main.rs` imports `tokio::time::interval`
- `agent-rs/src/main.rs` contains `tokio::select!` bridging `stream.next()` and `flush_interval.tick()`
- `cargo build` exits with 0
</acceptance_criteria>
</task>
