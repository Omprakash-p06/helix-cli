---
wave: 1
depends_on: []
files_modified:
  - agent-rs/src/input.rs
  - tests/test_accuracy.py
autonomous: true
---

# Plan 08-01: Terminal Submission & Accuracy Script

## Objective
Fix the "double enter" friction in the terminal and provide a programmatic way to verify tool-calling accuracy.

## Requirements Addressed
- UX-02: The terminal input loop must provide an intuitive submission method (e.g., Enter for send).
- TEST-01: The repository must include an automated evaluation script to test tool-calling accuracy.

## Tasks

<task>
<read_first>
- agent-rs/src/input.rs
</read_first>
<action>
Refactor the `Validator` implementation for `MultiLineHelper` in `input.rs` to return `ValidationResult::Valid(None)` immediately on enter, removing the check for a trailing newline.
</action>
<acceptance_criteria>
Manual test of terminal input submits on first Enter press.
</acceptance_criteria>
</task>

<task>
<read_first>
- tests/test_accuracy.py
</read_first>
<action>
Create or update `tests/test_accuracy.py` with a test suite that sends `tools` payloads to the local server and asserts that the response contains the expected `tool_calls`.
</action>
<acceptance_criteria>
`python tests/test_accuracy.py` executes and reports Pass/Fail results.
</acceptance_criteria>
</task>

## Verification
1. Run `cargo run` in `agent-rs` and verify single-enter submission.
2. Run `python tests/test_accuracy.py` with the server running.
