---
phase: 14-fix-tui-missing-output-bug
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - agent-rs/src/main.rs
  - agent-rs/src/tui.rs
  - agent-rs/src/stream.rs
autonomous: true
requirements: [UX-01, TEST-01]
must_haves:
  truths:
    - "When the model streams text, users see assistant output in TUI instead of a blank response area"
    - "Streaming remains responsive during generation and still supports interrupt handling"
    - "Tool-call deltas and content deltas are both parsed even when SSE frames arrive fragmented"
  artifacts:
    - path: "agent-rs/src/stream.rs"
      provides: "Robust SSE frame parser that supports partial chunks and emits complete data payloads"
    - path: "agent-rs/src/main.rs"
      provides: "Shared parser integration for terminal and TUI streaming loops"
    - path: "agent-rs/src/tui.rs"
      provides: "TUI event flow that receives and renders token chunks produced by the parsed stream"
  key_links:
    - from: "agent-rs/src/main.rs"
      to: "agent-rs/src/stream.rs"
      via: "stream parser state used in bytes_stream loop"
      pattern: "push_bytes|next_event|data:"
    - from: "agent-rs/src/main.rs"
      to: "agent-rs/src/tui.rs"
      via: "TuiEvent::TokenChunk and TuiEvent::ResponseDone"
      pattern: "event_tx.send\(tui::TuiEvent::TokenChunk"
---

<objective>
Fix the missing-output regression in terminal TUI streaming by making SSE parsing resilient to chunk boundaries and wiring parsed deltas consistently into token rendering.

Purpose: Ensure users always see generated output in real time, even when HTTP chunks split SSE lines.
Output: A reusable stream parser + integrated streaming loops + regression tests for fragmented SSE payloads.
</objective>

<execution_context>
@$HOME/.copilot/get-shit-done/workflows/execute-plan.md
@$HOME/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@agent-rs/src/main.rs
@agent-rs/src/tui.rs

<interfaces>
From agent-rs/src/tui.rs:
```rust
pub enum TuiEvent {
    TokenChunk(String),
    ToolStart(ToolInfo),
    ToolResult(ToolResultInfo),
    ResponseDone,
    Status(String),
    StatusUpdate(String),
    SystemMessage(String),
    GenerationStarted,
    ContextUpdate(usize, usize),
    ClearHistory,
}
```

From agent-rs/src/main.rs:
```rust
async fn run_llm_loop_tui(
    client: &Client,
    url: &str,
    app_config: &config::AppConfig,
    messages: &mut Vec<ChatMessage>,
    tools_payload: &Value,
    generated_grammar: &str,
    is_chat_mode: bool,
    server_flavor: ServerFlavor,
    action_rx: &mut tokio::sync::mpsc::UnboundedReceiver<tui::TuiAction>,
    event_tx: &tokio::sync::mpsc::UnboundedSender<tui::TuiEvent>,
)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a chunk-safe SSE parser contract and tests</name>
  <files>agent-rs/src/stream.rs</files>
  <behavior>
    - Test 1: SSE content split across arbitrary byte chunks still yields complete parsed data payloads.
    - Test 2: Multiple `data:` lines in one chunk are emitted in order.
    - Test 3: `[DONE]` sentinel is surfaced as terminal event and does not corrupt buffered partial lines.
    - Test 4: CRLF and LF line endings are both accepted.
  </behavior>
  <action>Create a dedicated SSE parser module that buffers partial lines across `bytes_stream()` boundaries, emits complete `data:` payloads only when line termination is reached, and exposes parser methods used by both terminal and TUI loops. Keep parsing logic deterministic and allocation-light. Do not parse JSON in this module; return payload strings only.</action>
  <verify>
    <automated>cd agent-rs && cargo test stream::tests:: -q</automated>
  </verify>
  <done>Parser tests prove fragmented SSE input is reconstructed correctly and `[DONE]` handling is explicit.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integrate the parser into both streaming loops and preserve tool-call assembly</name>
  <files>agent-rs/src/main.rs</files>
  <behavior>
    - Test 1: Content deltas parsed from fragmented SSE frames append to assistant output.
    - Test 2: Tool call deltas remain assembled by index/name/arguments while using parser output.
    - Test 3: TUI path emits `TokenChunk` during generation and `ResponseDone` at completion.
  </behavior>
  <action>Refactor both the classic terminal stream loop and `run_llm_loop_tui()` to consume parsed payloads from `stream.rs` instead of iterating `text.lines()` per raw chunk. Keep existing logic for content accumulation, tool_call delta stitching, and flush interval batching. Ensure `GenerationStarted` only fires on first parsed payload carrying a content delta. Preserve interrupt behavior and final token flush.</action>
  <verify>
    <automated>cd agent-rs && cargo test run_llm_loop_tui -- --nocapture</automated>
  </verify>
  <done>Both loops are parser-backed, no token loss occurs on split SSE lines, and tool-calling behavior is unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Add regression guardrails and end-to-end compile verification</name>
  <files>agent-rs/src/main.rs, agent-rs/src/tui.rs, agent-rs/src/stream.rs</files>
  <action>Add focused regression tests for the previously failing case where a `data:` JSON frame is split mid-line and only completed in a later chunk. Include one fixture where no newline appears in the first chunk. Ensure TUI rendering path receives non-empty `TokenChunk` in this scenario. Keep runtime logs minimal and behind debug-only guards if added.</action>
  <verify>
    <automated>cd agent-rs && cargo test -q && cargo check -q</automated>
  </verify>
  <done>Regression tests fail before the fix and pass after integration; crate compiles cleanly.</done>
</task>

</tasks>

<verification>
- Run full Rust tests and compile checks in `agent-rs`.
- Manually confirm `HELIX_UI_MODE=tui` displays a visible response for `hello!` while generation is active.
- Confirm Ctrl+C interrupt still stops generation and leaves partial streamed text visible.
</verification>

<success_criteria>
- `hello!` in TUI shows streamed assistant text instead of blank output.
- No regressions in tool-call parsing during streamed responses.
- SSE parsing logic is centralized and unit-tested against fragmented inputs.
</success_criteria>

<output>
After completion, create `.planning/phases/14-fix-tui-missing-output-bug/14-01-SUMMARY.md`
</output>
