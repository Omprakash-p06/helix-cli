---
phase: 14-fix-tui-missing-output-bug
plan: 02
type: execute
wave: 1
depends_on:
  - 14-01
files_modified:
  - agent-rs/src/main.rs
  - agent-rs/src/tui.rs
autonomous: true
requirements: [UX-01, TEST-01]
gap_closure: true
must_haves:
  truths:
    - "Users can see live generation progress even when backend emits non-content/tool-call deltas first"
    - "Tool-call phases no longer look frozen in TUI while model is still streaming"
    - "Interrupting generation preserves already-buffered partial output"
  artifacts:
    - path: "agent-rs/src/main.rs"
      provides: "Unified visible streaming extraction and guaranteed token-buffer flush on interrupt/exit"
    - path: "agent-rs/src/tui.rs"
      provides: "Renderable heartbeat/status event path for tool-only or non-content streaming windows"
  key_links:
    - from: "agent-rs/src/main.rs"
      to: "agent-rs/src/tui.rs"
      via: "TuiEvent emissions for token chunks, streaming heartbeat, and response completion"
      pattern: "event_tx\.send\(tui::TuiEvent::"
    - from: "agent-rs/src/main.rs"
      to: "token_buffer flush path"
      via: "interrupt and stream-end exit branches"
      pattern: "if !token_buffer\.is_empty\(\)"
---

<objective>
Close diagnosed UAT gaps where generation appears stuck by making streaming visibility robust across non-content deltas, tool-call phases, and interrupts.

Purpose: Ensure user-visible progress throughout generation and preserve partial output when interrupted.
Output: Streaming loop and TUI event/render improvements with regression checks.
</objective>

<execution_context>
@$HOME/.copilot/get-shit-done/workflows/execute-plan.md
@$HOME/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-fix-tui-missing-output-bug/14-UAT.md
@.planning/phases/14-fix-tui-missing-output-bug/14-01-SUMMARY.md
@agent-rs/src/main.rs
@agent-rs/src/tui.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expand visible streaming extraction beyond content-only deltas</name>
  <read_first>agent-rs/src/main.rs, agent-rs/src/stream.rs, .planning/phases/14-fix-tui-missing-output-bug/14-UAT.md</read_first>
  <files>agent-rs/src/main.rs</files>
  <behavior>
    - Test 1: When streamed chunks contain non-empty text in keys other than `delta.content` (for example reasoning/text fields), visible output is still appended in real time.
    - Test 2: GenerationStarted is emitted on first visible text emission (not only strict content key), preventing silent long waits.
    - Test 3: Existing tool_call assembly behavior remains unchanged.
  </behavior>
  <action>Implement a small helper in main.rs that extracts user-visible token text from the streamed JSON delta in strict priority order: `content`, then additional text-like fields used by local backends (for example `reasoning_content`, `text`, or equivalent string payloads). Use this helper in both terminal and TUI streaming loops so token rendering is no longer gated only to `delta.content` (addresses UAT gaps from tests 3 and 5). Keep tool_call stitching logic exactly as-is and do not parse tool_call JSON through the visible-text helper.</action>
  <acceptance_criteria>
    - `agent-rs/src/main.rs` contains a shared helper/function used by both stream loops to extract visible token text.
    - `agent-rs/src/main.rs` no longer gates all visible token emission on `delta.get("content")` only.
    - `cargo check -q` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd agent-rs && cargo check -q</automated>
  </verify>
  <done>Both loops can emit live visible output even when initial streamed deltas are non-content text payloads.</done>
</task>

<task type="auto">
  <name>Task 2: Add explicit TUI heartbeat feedback for tool-only/non-content streaming windows</name>
  <read_first>agent-rs/src/main.rs, agent-rs/src/tui.rs, .planning/phases/14-fix-tui-missing-output-bug/14-UAT.md</read_first>
  <files>agent-rs/src/main.rs, agent-rs/src/tui.rs</files>
  <action>Add a lightweight streaming heartbeat event path that fires while the model stream is active but no renderable token chunk has yet been emitted (addresses UAT gap from test 4). In `main.rs`, emit periodic heartbeat/status events during tool_call-only delta accumulation. In `tui.rs`, render this heartbeat in a user-visible way in the chat area (not status bar only), so users see progress instead of an apparently frozen screen.</action>
  <acceptance_criteria>
    - `agent-rs/src/tui.rs` defines and handles a dedicated event/state path for streaming heartbeat/progress.
    - `agent-rs/src/main.rs` emits that event while stream is active and token chunk is empty.
    - TUI render branch contains visible output for heartbeat/progress during generation.
  </acceptance_criteria>
  <verify>
    <automated>cd agent-rs && cargo check -q</automated>
  </verify>
  <done>Tool-call or non-content phases show active feedback in TUI instead of appearing stuck.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Flush buffered tokens on interrupt and add regression coverage</name>
  <read_first>agent-rs/src/main.rs, agent-rs/src/stream.rs, .planning/phases/14-fix-tui-missing-output-bug/14-UAT.md</read_first>
  <files>agent-rs/src/main.rs, agent-rs/src/stream.rs</files>
  <behavior>
    - Test 1: Interrupt path flushes any pending token_buffer into UI before exiting generation loop.
    - Test 2: Stream-end and interrupt paths both preserve partial visible output.
  </behavior>
  <action>In the TUI streaming select loop interrupt branch, flush `token_buffer` via `TuiEvent::TokenChunk` before breaking. Ensure this flush behavior is mirrored anywhere generation exits early. Add a focused regression unit/integration test around parser-loop behavior proving buffered text is not lost on early termination (addresses UAT gap from test 5).</action>
  <acceptance_criteria>
    - `agent-rs/src/main.rs` interrupt branch includes token_buffer flush before break.
    - A regression test exists that fails if buffered tokens are dropped on early exit.
    - `cd agent-rs && cargo test -q` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd agent-rs && cargo test -q</automated>
  </verify>
  <done>Interrupt no longer drops partial streamed output, and regression test protects against reintroduction.</done>
</task>

</tasks>

<verification>
- `cd agent-rs && cargo test -q && cargo check -q`
- Manual smoke check in TUI: ask for a tool-invoking prompt and confirm visible progress appears before final answer.
- Manual interrupt check: run long prompt, press Ctrl+C mid-generation, confirm partial output remains visible.
</verification>

<success_criteria>
- No UAT report of blank/stuck generation when model is actively streaming.
- Tool-call windows show live progress indicators.
- Interrupt preserves streamed partial output.
</success_criteria>

<output>
After completion, create `.planning/phases/14-fix-tui-missing-output-bug/14-02-SUMMARY.md`
</output>
