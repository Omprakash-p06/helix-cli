---
phase: 04
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - agent-rs/src/agent_core/mod.rs
  - agent-rs/src/agent_core/orchestration/mod.rs
  - agent-rs/src/agent_core/orchestration/phase_state.rs
  - agent-rs/src/agent_core/orchestration/artifacts.rs
autonomous: true
requirements: [GSD-01]
must_haves:
  truths:
    - "Phase state machine correctly handles transitions between all 6 GSD phases"
    - "Phase artifacts are successfully persisted to and loaded from disk"
    - "Orchestration module is correctly integrated into the agent_core"
  artifacts:
    - path: "agent-rs/src/agent_core/orchestration/phase_state.rs"
      provides: "GSD 2.0 Phase FSM"
    - path: "agent-rs/src/agent_core/orchestration/artifacts.rs"
      provides: "Artifact persistence logic"
  key_links:
    - from: "agent-rs/src/agent_core/mod.rs"
      to: "agent-rs/src/agent_core/orchestration/mod.rs"
      via: "pub mod orchestration"
---

<objective>
Establish the core orchestration scaffold for GSD 2.0 in the helix-agent.
This plan focuses on the structural and state management components of the orchestration layer.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-gsd-2.0-integration-autonomous-workflows/04-RESEARCH.md
@agent-rs/src/agent_core/mod.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Orchestration Module Scaffold</name>
  <files>
    agent-rs/src/agent_core/mod.rs,
    agent-rs/src/agent_core/orchestration/mod.rs,
    agent-rs/src/agent_core/orchestration/phase_state.rs,
    agent-rs/src/agent_core/orchestration/artifacts.rs
  </files>
  <action>
    - Create the directory `agent-rs/src/agent_core/orchestration/`.
    - Update `agent-rs/src/agent_core/mod.rs` to include `pub mod orchestration;`.
    - Initialize `agent-rs/src/agent_core/orchestration/mod.rs` with placeholders for the orchestrator adapter.
    - Create empty files for `phase_state.rs` and `artifacts.rs`.
  </action>
  <verify>
    <automated>ls agent-rs/src/agent_core/orchestration/mod.rs agent-rs/src/agent_core/orchestration/phase_state.rs agent-rs/src/agent_core/orchestration/artifacts.rs</automated>
  </verify>
  <done>Module structure created and integrated into agent_core.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Phase FSM</name>
  <files>agent-rs/src/agent_core/orchestration/phase_state.rs</files>
  <action>
    Implement the GSD 2.0 Phase State Machine in `phase_state.rs`:
    - Define `Phase` enum: `Discover`, `Discuss`, `Plan`, `Execute`, `Verify`, `Close`.
    - Implement a `PhaseStateMachine` struct that manages transitions.
    - Include logic for valid transition checking (e.g., cannot go from `Discover` directly to `Execute`).
    - Add unit tests for state transitions.
  </action>
  <verify>
    <automated>cargo test orchestration::phase_state</automated>
  </verify>
  <done>Phase FSM implemented with transition validation and unit tests.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Artifact Persistence</name>
  <files>agent-rs/src/agent_core/orchestration/artifacts.rs</files>
  <action>
    Implement artifact management in `artifacts.rs`:
    - Define `PhaseArtifact` trait or enum for different phase outputs (Plan, ExecutionReceipt, VerifyResult).
    - Implement functions to save and load artifacts to/from `.planning/phases/{padded_phase}-{slug}/` (per pattern in RESEARCH.md).
    - Ensure artifacts are serialized using `serde_json`.
    - Use `tokio::fs` for async file I/O.
  </action>
  <verify>
    <automated>cargo test orchestration::artifacts</automated>
  </verify>
  <done>Artifact persistence logic implemented and tested.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Orchestration -> Filesystem | Orchestrator writes artifacts to the host filesystem outside the sandbox. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Tampering | artifacts.rs | mitigate | Use standard serialization and ensure atomic writes to prevent corrupted artifacts. |
| T-04-01-02 | Information Disclosure | artifacts.rs | accept | Artifacts contain phase plans and execution results, which are intended to be stored on disk. |
</threat_model>

<verification>
- Phase state transition correctness unit tests.
- Artifact serialization/deserialization and disk I/O unit tests.
</verification>

<success_criteria>
- Module scaffold exists and is compiled.
- `cargo test orchestration::` passes.
</success_criteria>

<output>
After completion, create `.planning/phases/04-gsd-2.0-integration-autonomous-workflows/04-01-SUMMARY.md`
</output>
