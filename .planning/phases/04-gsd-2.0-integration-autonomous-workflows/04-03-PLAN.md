---
phase: 04
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - agent-rs/src/agent_core/orchestration/mod.rs
  - agent-rs/src/tui/commands.rs
  - agent-rs/src/main.rs
autonomous: true
requirements: [GSD-01]
must_haves:
  truths:
    - "Slash commands /gsd plan and /gsd execute are available and functional"
    - "Orchestration telemetry is visible in the TUI during execution"
  artifacts:
    - path: "agent-rs/src/agent_core/orchestration/mod.rs"
      provides: "Orchestrator Adapter and advance_phase implementation"
  key_links:
    - from: "agent-rs/src/main.rs"
      to: "agent-rs/src/agent_core/orchestration/mod.rs"
      via: "Command routing"
---

<objective>
Connect the orchestration layer to the agent's entry points and UI, enabling multi-phase autonomous workflows. This plan focuses on wiring and routing.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-gsd-2.0-integration-autonomous-workflows/04-RESEARCH.md
@agent-rs/src/tui/commands.rs
@agent-rs/src/main.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register GSD Slash Commands</name>
  <files>agent-rs/src/tui/commands.rs</files>
  <action>
    Register the new slash commands in `tui/commands.rs`:
    - Add `gsd_plan` and `gsd_execute` commands to `default_commands()`.
    - Map these commands to `TuiAction::SystemCommand` in `execute_command()`.
    - Provide examples: `/gsd plan "fix network issues"` and `/gsd execute`.
  </action>
  <verify>
    <automated>cargo test tui::commands</automated>
  </verify>
  <done>GSD slash commands registered and searchable in the TUI.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Orchestrator Adapter</name>
  <files>agent-rs/src/agent_core/orchestration/mod.rs</files>
  <action>
    Implement the public API for orchestration in `mod.rs`:
    - Implement the `advance_phase` function as defined in RESEARCH.md.
    - This function should:
      1. Load artifacts.
      2. Rebuild context (using `ContextResetter`).
      3. Execute phase logic (calling existing tool runtime if in `Execute` phase).
      4. Handle failures (using `RecoveryOperator`).
      5. Persist artifacts and return the next phase.
    - Wire phase execution telemetry to existing `ToolLifecycle` events.
  </action>
  <verify>
    <automated>cargo test orchestration::mod</automated>
  </verify>
  <done>Orchestrator adapter implemented, unifying phase transitions and execution.</done>
</task>

<task type="auto">
  <name>Task 3: Main Loop Command Routing</name>
  <files>agent-rs/src/main.rs</files>
  <action>
    Update the command handling logic in `main.rs`:
    - Catch `/gsd plan` and `/gsd execute` system commands.
    - Delegate execution to the `orchestration::advance_phase` function.
    - Ensure results and telemetry are streamed back to the TUI.
  </action>
  <verify>
    <automated>cargo check</automated>
  </verify>
  <done>Main execution loop updated to route GSD commands to the orchestration engine.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| User -> Orchestrator | User inputs slash commands that trigger multi-step autonomous workflows. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | Elevation of Privilege | orchestration/mod.rs | mitigate | Ensure that orchestration cannot bypass existing policy and HITL checks in `ToolRuntime`. |
| T-04-03-02 | Repudiation | orchestration/mod.rs | mitigate | Record all phase transitions and outcomes in the append-only audit log. |
</threat_model>

<verification>
- Command registration verification.
- Telemetry propagation tests.
- Compilation and basic routing check.
</verification>

<success_criteria>
- `/gsd` commands are functional in the TUI.
- `advance_phase` logic correctly routes between core orchestration components.
</success_criteria>

<output>
After completion, create `.planning/phases/04-gsd-2.0-integration-autonomous-workflows/04-03-SUMMARY.md`
</output>
