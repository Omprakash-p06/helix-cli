---
phase: 04
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - agent-rs/src/agent_core/orchestration/context_reset.rs
  - agent-rs/src/agent_core/orchestration/recovery.rs
autonomous: true
requirements: [GSD-02, GSD-03]
must_haves:
  truths:
    - "Prompts can be reconstructed from phase artifacts to prevent context rot"
    - "Autonomous recovery operators (RETRY, DECOMPOSE, PRUNE) handle verification failures"
    - "Loop detection correctly identifies and escalates repeating failure cycles"
  artifacts:
    - path: "agent-rs/src/agent_core/orchestration/context_reset.rs"
      provides: "Context reset logic"
    - path: "agent-rs/src/agent_core/orchestration/recovery.rs"
      provides: "Autonomous recovery operators and loop detector"
  key_links:
    - from: "agent-rs/src/agent_core/orchestration/context_reset.rs"
      to: "agent-rs/src/agent_core/orchestration/artifacts.rs"
      via: "Artifact reading"
---

<objective>
Implement the autonomous capabilities of the orchestration layer, including context management and error recovery.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-gsd-2.0-integration-autonomous-workflows/04-RESEARCH.md
@agent-rs/src/agent_core/orchestration/artifacts.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Context Reset Logic</name>
  <files>agent-rs/src/agent_core/orchestration/context_reset.rs</files>
  <action>
    Implement context reset mechanism in `context_reset.rs`:
    - Create logic to rebuild LLM prompts using only persisted phase artifacts and current system state (per Pitfall 1 in RESEARCH.md).
    - Implement a `ContextResetter` that clears the chat history and injects the reconstructed prompt.
    - Ensure it respects phase boundaries (e.g., Plan phase context only includes results from Discover and Discuss).
  </action>
  <verify>
    <automated>cargo test orchestration::context_reset</automated>
  </verify>
  <done>Context reset logic implemented to prevent context rot across multi-step sessions.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Autonomous Recovery Operators</name>
  <files>agent-rs/src/agent_core/orchestration/recovery.rs</files>
  <action>
    Implement recovery operators in `recovery.rs`:
    - Implement the recovery decision matrix: `RETRY` (bounded), `DECOMPOSE` (split step), `PRUNE` (skip optional).
    - Add a `LoopDetector` that uses fingerprints (tool + args hash + outcome) to identify repeating failure cycles (per Pattern 3 and Pitfall 3 in RESEARCH.md).
    - Implement escalation logic when max attempts or loop detection triggers.
  </action>
  <verify>
    <automated>cargo test orchestration::recovery</automated>
  </verify>
  <done>Autonomous recovery operators and loop detection implemented with unit tests.</done>
</task>

<task type="auto">
  <name>Task 3: Verification of Recovery Logic</name>
  <files>agent-rs/src/agent_core/orchestration/recovery.rs</files>
  <action>
    Add comprehensive tests for recovery scenarios:
    - Test `RETRY` limit enforcement.
    - Test `DECOMPOSE` triggering after `RETRY` exhaustion.
    - Test `PRUNE` for optional steps.
    - Test `LoopDetector` with various failure signatures.
  </action>
  <verify>
    <automated>cargo test orchestration::recovery::tests</automated>
  </verify>
  <done>Recovery logic verified through exhaustive unit testing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Recovery Logic -> Execution | The recovery operator determines the next execution attempt. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | Denial of Service | recovery.rs | mitigate | Enforce strict caps on retries and use signature-based loop detection to prevent infinite execution loops. |
| T-04-02-02 | Tampering | context_reset.rs | mitigate | Reconstruct prompts only from trusted, persisted artifacts to prevent context poisoning. |
</threat_model>

<verification>
- Context reset boundary tests.
- Recovery operator decision matrix unit tests.
- Loop detector escalation tests.
</verification>

<success_criteria>
- Context reset logic successfully rebuilds prompts from artifacts.
- Recovery logic correctly handles simulated verification failures.
- `cargo test orchestration::` passes.
</success_criteria>

<output>
After completion, create `.planning/phases/04-gsd-2.0-integration-autonomous-workflows/04-02-SUMMARY.md`
</output>
