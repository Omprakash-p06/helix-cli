---
phase: 04
plan: 04
type: execute
wave: 4
depends_on: ["04-03"]
files_modified:
  - agent-rs/tests/gsd_orchestration_validation.rs
autonomous: false
requirements: [GSD-01, GSD-02, GSD-03]
must_haves:
  truths:
    - "Agent can perform a full multi-phase orchestration flow from Discover to Close"
    - "Full orchestration flow passes comprehensive integration tests"
    - "Protocol validation ensures artifacts and transitions match GSD 2.0 standards"
  artifacts:
    - path: "agent-rs/tests/gsd_orchestration_validation.rs"
      provides: "Integration tests for full GSD flow"
  key_links:
    - from: "agent-rs/tests/gsd_orchestration_validation.rs"
      to: "agent-rs/src/agent_core/orchestration/mod.rs"
      via: "Integration testing"
---

<objective>
Validate the complete GSD 2.0 orchestration implementation through end-to-end integration testing and protocol verification.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-gsd-2.0-integration-autonomous-workflows/04-RESEARCH.md
@.planning/phases/04-gsd-2.0-integration-autonomous-workflows/04-VALIDATION.md
@agent-rs/src/agent_core/orchestration/mod.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full Flow Integration Test</name>
  <files>agent-rs/tests/gsd_orchestration_validation.rs</files>
  <action>
    Create a comprehensive integration test in `tests/gsd_orchestration_validation.rs`:
    - Simulate a full repair workflow from `Discover` to `Close`.
    - Mock LLM responses for each phase.
    - Verify that artifacts are created and context resets occur at each boundary.
    - Verify that a simulated failure in `Execute` triggers a `RETRY` or `DECOMPOSE` cycle.
  </action>
  <verify>
    <automated>cargo test --test gsd_orchestration_validation</automated>
  </verify>
  <done>Full orchestration flow verified with end-to-end integration tests.</done>
</task>

<task type="auto">
  <name>Task 2: GSD 2.0 Protocol Validation</name>
  <files>agent-rs/tests/gsd_orchestration_validation.rs</files>
  <action>
    Implement specific protocol validation checks:
    - Verify that phase artifacts strictly follow the schema defined in `artifacts.rs`.
    - Verify that the `AuditStore` records all state transitions correctly.
    - Verify that the context rebuild logic produces prompts that contain the necessary historical artifacts.
  </action>
  <verify>
    <automated>cargo test --test gsd_orchestration_validation -- --nocapture</automated>
  </verify>
  <done>GSD 2.0 protocol adherence validated through rigorous testing.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Full GSD 2.0 orchestration integration with autonomous recovery and protocol validation.</what-built>
  <how-to-verify>
    1. Run the agent: `cargo run`.
    2. Enter `/gsd plan "simulate a file permission repair"`.
    3. Verify the agent transitions through Discuss/Plan phases and shows progress in the TUI.
    4. Run `/gsd execute`.
    5. Verify the agent attempts the repair and provides a summary.
    6. (Optional) Force a verification failure (e.g., by changing a file during the execute phase) and observe the recovery operator initiating a `RETRY`.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Test Framework -> Agent | Tests simulate user and LLM interactions to verify internal logic. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04-01 | Tampering | artifacts | mitigate | Validation tests ensure that artifacts are not corrupted during state transitions. |
| T-04-04-02 | Repudiation | audit log | mitigate | Verification confirms that every transition is reliably logged in the audit trail. |
</threat_model>

<verification>
- Full suite of orchestration integration tests.
- Protocol adherence check.
- Human-in-the-loop functional verification.
</verification>

<success_criteria>
- All integration tests pass.
- End-to-end flow is demonstrable in the TUI.
- Phase 04 requirements (GSD-01, GSD-02, GSD-03) are fully met and verified.
</success_criteria>

<output>
After completion, create `.planning/phases/04-gsd-2.0-integration-autonomous-workflows/04-04-SUMMARY.md`
</output>
