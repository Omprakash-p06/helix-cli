---
phase: 05-model-management-ux-polish
plan: 03
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - agent-rs/src/tui.rs
  - agent-rs/src/tui/commands.rs
  - agent-rs/src/main.rs
autonomous: true
requirements:
  - UX-01

must_haves:
  truths:
    - GSD commands are recognized (plan, execute, verify, discuss, next)
    - Input field shows suggested command as user types /
    - Tab or Right arrow accepts suggestion
  artifacts:
    - path: agent-rs/src/tui.rs
      provides: Input handling and ghost suggestion display
    - path: agent-rs/src/tui/commands.rs
      provides: Slash command registry and matching
    - path: agent-rs/src/main.rs
      provides: Orchestration state for suggestion logic
  key_links:
    - from: tui.rs
      to: commands.rs
      via: CommandRegistry::match_partial()
      pattern: match_partial.*prefix
---

<objective>
**Phase 05 Plan 03:** GSD Message Autofill

Implement slash-command UX with ghost suggestions and autofill for GSD commands.

**Purpose:** User can quickly issue GSD commands via suggestion; typing "/p" suggests "/gsd plan" which they accept with Tab.

**Output:** Slash commands detected, suggestions shown, Tab/Right accepts suggestion
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-model-management-ux-polish/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend slash command registry</name>
  <files>agent-rs/src/tui/commands.rs</files>
  <action>
Extend the command registry for GSD autofill:
1. Add GSD commands to registry: /gsd plan, /gsd execute, /gsd verify, /gsd discuss, /gsd next, /gsd status
2. Add variant matching: "/p" matches "/gsd plan", "/e" matches "/gsd execute"
3. Add descriptions for each command (shown on help)
4. Make registry extensible for future commands

This extends existing slash-command handling per RESEARCH.md: "GSD command suggestions - A second parser or hint engine - Existing slash-command registry and ghost suggestion logic"

Pattern: existing in agent-rs/src/tui/commands.rs
</action>
  <verify>
<automated>grep -n "gsd.*plan\|gsd.*execute" agent-rs/src/tui/commands.rs | head -3</automated>
  </verify>
  <done>
- GSD commands registered
- Partial match works "/p" -> "/gsd plan"
- Descriptions available for help display
</done>
</task>

<task type="auto">
<name>Task 2: Implement ghost suggestion rendering</name>
<files>agent-rs/src/tui.rs</files>
<action>
Add ghost suggestion display to TUI input:
1. Track current input as user types
2. On "/" detection, look up matching commands via CommandRegistry
3. Display suggestion in muted color (ghost text) after cursor
4. Position suggestion inline after partial input
5. Clear suggestion on non-slash input

Research notes from RESEARCH.md:
- "User can see existing message" requires UI component state
- Ghost suggestion state lives in TUI, not in backend
</action>
<verify>
<automated>grep -n "suggestion\|ghost" agent-rs/src/tui.rs | head -5</automated>
</verify>
<done>
- Suggestion text appears after partial "/"
- Muted color distinguishes from actual input
- Suggestion clears correctly
</done>
</task>

<task type="auto">
<name>Task 3: Implement suggestion acceptance</name>
<files>agent-rs/src/tui.rs</files>
<action>
Handle Tab/Right arrow to accept suggestion:
1. When suggestion is showing and user presses Tab OR Right arrow:
   - Replace partial input with full command
   - Clear suggestion state
2. Allow Tab for completion + submit (optional config)
3. Preserve typed input if suggestion dismissed (Escape)
4. Escape clears suggestion without clearing input

Default: Tab accepts, Escape clears suggestion only.
</action>
<verify>
<automated>grep -n "KeyEvent.*Tab\|accept.*suggestion" agent-rs/src/tui.rs | head -3</automated>
</verify>
<done>
- Tab accepts ghost suggestion
- Right arrow also accepts
- Escape dismisses without clearing typed text
</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user_input→command_parse | Input parsed crosses from UI to logic layer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07 | Spoofing | fake suggestion | accept | Suggestions only from registry; no user content |
| T-05-08 | Tampering | command injection | mitigate | Commands validated before execution; no arbitrary shell |
</threat_model>

<verification>
- [ ] Registry contains GSD commands
- [ ] Ghost suggestion appears after "/"
- [ ] Tab accepts suggestion
</verification>

<success_criteria>
- User types "/" and sees suggested completions
- Tab or Right arrow accepts suggestion
- Original typed input preserved on escape
</success_criteria>

<output>
After completion, create `.planning/phases/05-model-management-ux-polish/05-03-SUMMARY.md`
</output>