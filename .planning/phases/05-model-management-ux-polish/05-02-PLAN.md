---
phase: 05-model-management-ux-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/download_model.py
  - scripts/model_install.py
autonomous: true
requirements:
  - MOD-03

must_haves:
  truths:
    - User can specify HF repository (e.g., "TheBloke/Mistral-7B-v0.1-GGUF")
    - User can select which file to download from available .gguf files
    - Download includes SHA256 verification before activation
  artifacts:
    - path: scripts/download_model.py
      provides: Interactive HF download flow with huggingface_hub
    - path: scripts/model_install.py
      provides: Staging, checksum verification, atomic activation
  key_links:
    - from: download_model.py
      to: huggingface_hub
      via: HfApi methods
      pattern: hf_hub_download|repo_info
---

<objective>
**Phase 05 Plan 02:** Hugging Face GGUF Downloader

Implement tool/command to download GGUF models directly from Hugging Face with SHA256 verification.

**Purpose:** Users can download models via command without manual file placement; integrity verified before use.

**Output:** Downloaded and verified GGUF files in models/ directory
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-model-management-ux-polish/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create HF repository browser</name>
  <files>scripts/download_model.py</files>
  <action>
Create `scripts/download_model.py` with Hugging Face integration:
1. Use `huggingface_hub` library (not raw HTTP)
2. Function `list_repo_files(repo_id)` returns available .gguf files
3. Function `list_repos_by_tag(tag)` searches for popular GGUF models
4. Display file size estimates before download prompts
5. Handle rate limiting and auth gracefully

Key patterns from RESEARCH.md:
- Use `huggingface_hub` for repo-tree listing: `HfApi().repo_info()`
- Use file_download helpers instead of manual get()
</action>
  <verify>
<automated>python3 -c "from scripts.download_model import list_repo_files; print(list_repo_files('TheBloke/Mistral-7B-v0.1-GGUF')[:3])"</automated>
  </verify>
  <done>
- list_repo_files() returns list of .gguf files with sizes
- Handles missing repos gracefully
- Uses huggingface_hub (not requests.get)
</done>
</task>

<task type="auto">
  <name>Task 2: Implement download with progress</name>
  <files>scripts/download_model.py</files>
  <action>
Add download functionality with progress tracking:
1. Use `huggingface_hub` `hf_hub_download()` for reliable downloads
2. Wrap with `tqdm` for progress bar display
3. Resume support for interrupted downloads (use cached)
4. Target location: models/ directory with proper subdirectory structure

Key patterns from RESEARCH.md:
- Progress: use `tqdm` wrapper around download
- Don't hand-roll: use Hub client for auth/retry/sha handling
</action>
  <verify>
<automated>python3 -c "from scripts.download_model import download_file; print('download_file function exists')"</automated>
  </verify>
  <done>
- Download shows progress bar
- Resume works from cache
- Correctly places file in models/ subdirectory
</done>
</task>

<task type="auto">
  <name>Task 3: Add SHA256 verification</name>
  <files>scripts/model_install.py</files>
  <action>
Verify download integrity before activation:
1. Existing model_install.py has SHA verification — extend it
2. Use `hashlib.sha256()` for file integrity check
3. Compare against expected SHA from HF metadata
4. If verification fails: delete partial, prompt to retry
5. After verification: move from staging to active models/

If HF doesn't provide SHA in metadata, use the Hub's sha256:
- Hub provides `sha256` in file metadata response
- Verify against computed hash
</action>
  <verify>
<automated>python3 -c "import hashlib; h = hashlib.sha256(); print('hashlib available:', h is not None)"</automated>
  </verify>
  <done>
- SHA verification runs automatically after download
- Failed verification removes corrupt file
- Success triggers "model ready" message
</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HF API→download | Network fetch crosses trust boundary |
| download→models/ | Files written to local filesystem |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Tampering | download path | mitigate | Verify SHA256 before moving to models/; atomic staging first |
| T-05-05 | DoS | large download | accept | User-initiated; timeout/ctrl-c supported |
| T-05-06 | Repudiation | download audit | accept | Log download intent to audit trail (from Phase 01) |
</threat_model>

<verification>
- [ ] list_repo_files() returns file list
- [ ] Progress shown during download
- [ ] SHA verification before activation
</verification>

<success_criteria>
- User can download by repo_id and filename
- Progress bar displays during download
- SHA verification fails gracefully on corrupt download
</success_criteria>

<output>
After completion, create `.planning/phases/05-model-management-ux-polish/05-02-SUMMARY.md`
</output>