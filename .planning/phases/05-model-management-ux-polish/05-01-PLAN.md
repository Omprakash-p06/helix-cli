---
phase: 05-model-management-ux-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/config.py
  - scripts/model_install.py
  - start.py
autonomous: true
requirements:
  - MOD-02

must_haves:
  truths:
    - User can see available models in models/ directory on startup
    - User can select which model to use if multiple models exist
    - Model selection persists through the launcher/server boundary
  artifacts:
    - path: scripts/config.py
      provides: Model discovery and catalog building
    - path: start.py
      provides: Startup model selection menu
    - path: scripts/model_install.py
      provides: Model staging and verification
  key_links:
    - from: start.py
      to: scripts/config.py
      via: build_model_entry() call
      pattern: build_model_entry.*model.*vram
---

<objective>
**Phase 05 Plan 01:** Dynamic Model Discovery & Startup Selection

Enable dynamic model discovery from `models/` directory and provide a startup model selection menu.

**Purpose:** Users can switch models without editing code; multiple GGUF models are discoverable and selectable.

**Output:** Models scanned and presented at startup; selection passed to runtime
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-model-management-ux-polish/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend model discovery in scripts/config.py</name>
  <files>scripts/config.py</files>
  <action>
Extend `scripts/config.py` to dynamically scan the `models/` directory on startup:
1. Add function `scan_models_directory()` that finds all .gguf files in models/
2. For each model, extract metadata (file size, suspected parameter count from filename)
3. Build model catalog with: name, path, size_mb, vram_estimate_gb
4. Return list of ModelEntry objects sorted by parameter count (ascending)
5. Handle empty models/ directory gracefully with clear message

Key patterns from RESEARCH.md:
- Single source of truth: resolve model once, pass through env vars
- Use pattern: `build_model_entry(model_name, detected_vram_gb)`
</action>
  <verify>
<automated>python3 -c "from scripts.config import scan_models_directory; models = scan_models_directory(); print(f'Found {len(models)} models')" 2>&1 | head -5</automated>
  </verify>
  <done>
- scan_models_directory() function exists in scripts/config.py
- Returns list of model entries with name, path, size_mb, vram_estimate_gb
- Handles empty directory without crashing
- Integration test: models appear in startup menu
</done>
</task>

<task type="auto">
  <name>Task 2: Add startup model selection menu</name>
  <files>start.py</files>
  <action>
Modify `start.py` to present model selection at startup:
1. If models/ contains 1 model: use it silently (current behavior)
2. If models/ contains 2+ models: present selection menu using inquire::Select
3. Allow user to skip selection and use default (prompt_skippable)
4. After selection, write HELIX_MODEL_NAME and HELIX_MODEL_PATH to env for server

Use inquire::Select pattern from RESEARCH.md:
```python
selected_model = Select::new("Choose a model", model_names)
    .prompt_skippable()?
```

Preserve fallback to environment variables if set externally.
</action>
  <verify>
<automated>python3 start.py --help 2>&1 | grep -i model</automated>
  </verify>
  <done>
- Startup shows selection when 2+ models exist
- Selected model passed to server via env vars
- Skip selection returns default (lowest vram model)
- No breaking changes to headless mode
</done>
</task>

<task type="auto">
  <name>Task 3: Verify model handoff through launcher</name>
  <files>scripts/start_server.py</files>
  <action>
Verify the runtime receives the correct model from selection:
1. start_server.py reads HELIX_MODEL_NAME and HELIX_MODEL_PATH from env
2. If env vars not set, falls back to auto-detection
3. Verify the server logs which model is being loaded at startup
4. Add startup log: "Loading model: {name} from {path}"
</action>
  <verify>
<automated>python3 -c "import os; os.environ['HELIX_MODEL_NAME']='test'; from scripts.start_server import get_model_config; print(get_model_config())"</automated>
  </verify>
  <done>
- Server logs model being loaded at startup
- Env override takes precedence over auto-detect
- Selection from start.py correctly passed to server
</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user→model_selection | User selects from list; no arbitrary code execution |
| model_files→catalog | Files scanned; no execution of model content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | model discovery | mitigate | Sanitize model filenames before using in paths; validate .gguf extension |
| T-05-02 | DoS | large directory scan | accept | models/ typically small; timeout reasonable at 30s |
| T-05-03 | Info disclosure | model filenames | accept | Filenames visible in UI only if user has terminal access |
</threat_model>

<verification>
- [ ] scan_models_directory() returns model list
- [ ] start.py shows selection when 2+ models
- [ ] Model handoff to server verified via env vars
</verification>

<success_criteria>
- User sees available models at startup
- Selection persists to running server
- Headless mode (no selection) works when env vars set externally
</success_criteria>

<output>
After completion, create `.planning/phases/05-model-management-ux-polish/05-01-SUMMARY.md`
</output>