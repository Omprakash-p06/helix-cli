---
phase: 26-open-source-integration-references
plan: 02
type: execute
wave: 2
depends_on:
  - 26-01
files_modified:
  - agent-rs/src/agent_core/mod.rs
  - agent-rs/src/agent_core/tool_runtime.rs
  - agent-rs/src/main.rs
  - agent-rs/src/server.rs
  - agent-rs/src/tui/api.rs
  - agent-rs/tests/tool_runtime_contracts.rs
autonomous: true
requirements:
  - TOOL-01
  - CODE-01
must_haves:
  truths:
    - "Tool execution uses a shared async runtime contract across terminal, TUI, and web paths."
    - "Tool lifecycle events are emitted consistently regardless of caller surface."
    - "Shared execution types/contracts are centralized instead of duplicated orchestration logic."
  artifacts:
    - path: "agent-rs/src/agent_core/mod.rs"
      provides: "Shared core module export surface for tool runtime contracts"
      min_lines: 20
    - path: "agent-rs/src/agent_core/tool_runtime.rs"
      provides: "Async tool executor contract, result envelope, and lifecycle event types"
      min_lines: 180
    - path: "agent-rs/src/main.rs"
      provides: "Terminal/TUI integration to shared tool runtime"
      min_lines: 2600
    - path: "agent-rs/src/server.rs"
      provides: "Web-path integration to shared tool runtime"
      min_lines: 500
    - path: "agent-rs/tests/tool_runtime_contracts.rs"
      provides: "Contract tests for async spawn, ordering, and timeout semantics"
      min_lines: 140
  key_links:
    - from: "agent-rs/src/main.rs"
      to: "agent-rs/src/agent_core/tool_runtime.rs"
      via: "shared async executor invocation"
      pattern: "join_all|execute_tool_async|ToolRuntime"
    - from: "agent-rs/src/server.rs"
      to: "agent-rs/src/agent_core/tool_runtime.rs"
      via: "web path dispatch through shared runtime"
      pattern: "ToolRuntime|tool lifecycle"
    - from: "agent-rs/src/tui/api.rs"
      to: "agent-rs/src/agent_core/tool_runtime.rs"
      via: "consistent lifecycle event contracts"
      pattern: "ToolStart|ToolResult|ToolStatus"
---

<objective>
Adopt open-source inspired execution architecture by centralizing async tool runtime contracts and lifecycle events into shared core modules.

Purpose: Complete non-blocking execution and codebase consolidation goals so all interaction surfaces use one tested tool runtime abstraction.
Output: Shared tool runtime module, unified integration points, and contract regression tests.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/26-open-source-integration-references/26-RESEARCH.md
@.planning/phases/17-non-blocking-tool-execution/17-02-SUMMARY.md
@agent-rs/src/main.rs
@agent-rs/src/server.rs
@agent-rs/src/tui/api.rs
@agent-rs/src/tools.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define shared async tool runtime contracts in agent_core</name>
  <files>agent-rs/src/agent_core/mod.rs, agent-rs/src/agent_core/tool_runtime.rs, agent-rs/tests/tool_runtime_contracts.rs</files>
  <behavior>
    - Shared runtime types define tool request envelopes, lifecycle events, and result metadata.
    - Async spawn + timeout behavior remains explicit and testable.
    - Contract tests validate ordering and timeout semantics independent of UI paths.
  </behavior>
  <action>Create a shared core tool runtime module and expose it through `agent_core` exports. Move duplicated execution-contract logic into this module while preserving current timeout and result-shape behavior.</action>
  <verify>
    <automated>cd agent-rs && cargo test -q --test tool_runtime_contracts</automated>
  </verify>
  <done>Shared runtime contracts compile and pass contract tests for ordering/timeouts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integrate terminal, TUI, and web paths with shared runtime abstraction</name>
  <files>agent-rs/src/main.rs, agent-rs/src/server.rs, agent-rs/src/tui/api.rs, agent-rs/src/agent_core/tool_runtime.rs</files>
  <behavior>
    - Terminal/TUI execution paths call shared runtime abstraction (no divergent tool scheduling logic).
    - Web `/chat` path uses the same lifecycle contract for start/result events.
    - Tool lifecycle messages remain compatible with existing UI/event consumers.
  </behavior>
  <action>Refactor orchestration code to invoke shared runtime contracts rather than ad hoc per-surface execution branches. Preserve existing policy gating and audit hooks while unifying async dispatch and event emission signatures.</action>
  <verify>
    <automated>cd agent-rs && cargo test -q --test tool_runtime_contracts</automated>
  </verify>
  <done>All runtime surfaces share one tool execution contract with stable lifecycle behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Full-suite verification for consolidated tool runtime</name>
  <files>agent-rs/src/main.rs, agent-rs/src/server.rs, agent-rs/src/agent_core/tool_runtime.rs</files>
  <action>Run full Rust test suite to verify consolidated runtime wiring does not regress security policy gating, streaming behavior, or tool execution correctness.</action>
  <verify>
    <automated>cd agent-rs && cargo test -q</automated>
  </verify>
  <done>Full Rust suite passes with unified runtime contracts in place.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tool policy gate -> shared runtime | Authorized calls cross into execution dispatcher |
| shared runtime -> terminal/web/TUI event consumers | Lifecycle signals cross subsystem boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-04 | E | `agent-rs/src/agent_core/tool_runtime.rs` | mitigate | Preserve policy pre-check call path and reject runtime execution when decision is deny/approval-required. |
| T-26-05 | D | shared async executor | mitigate | Keep per-tool timeout enforcement and non-blocking join patterns; add contract tests for hung tool scenarios. |
| T-26-06 | R | cross-surface lifecycle events | mitigate | Use one typed event contract and regression tests so terminal/web/TUI produce auditable equivalent lifecycle states. |
</threat_model>

<verification>
- `cd agent-rs && cargo test -q --test tool_runtime_contracts` passes.
- `cd agent-rs && cargo test -q` passes.
</verification>

<success_criteria>
- TOOL-01 is satisfied through shared async non-blocking tool runtime usage across all surfaces.
- CODE-01 is advanced by centralizing runtime contracts into shared core modules instead of duplicated orchestration code.
- Runtime behavior remains stable under full test verification.
</success_criteria>

<output>
After completion, create `.planning/phases/26-open-source-integration-references/26-02-SUMMARY.md`
</output>
