---
phase: 26-open-source-integration-references
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - agent-rs/src/main.rs
  - agent-rs/src/stream.rs
  - agent-rs/src/tui.rs
  - agent-rs/src/tui/state.rs
  - agent-rs/src/tui/api.rs
  - agent-rs/tests/streaming_tui_refinement.rs
autonomous: true
requirements:
  - UX-01
  - STREAM-01
must_haves:
  truths:
    - "Users see token streaming updates without timer-batched pauses in TUI mode."
    - "Thinking and tool progress blocks remain readable while streaming is active."
    - "Rich TUI tool timeline entries can be expanded/collapsed with stable metadata rendering."
  artifacts:
    - path: "agent-rs/src/main.rs"
      provides: "Byte-level streaming flow in TUI loop without 30ms batch gating"
      min_lines: 2600
    - path: "agent-rs/src/stream.rs"
      provides: "SSE parsing helpers for chunk-safe event extraction"
      min_lines: 120
    - path: "agent-rs/src/tui.rs"
      provides: "Rendering and event handling for immediate token/tool updates"
      min_lines: 1200
    - path: "agent-rs/src/tui/state.rs"
      provides: "Durable collapsible tool timeline state"
      min_lines: 180
    - path: "agent-rs/tests/streaming_tui_refinement.rs"
      provides: "Regression tests for immediate streaming and widget behavior"
      min_lines: 120
  key_links:
    - from: "agent-rs/src/main.rs"
      to: "agent-rs/src/stream.rs"
      via: "SSE bytes_stream event decoding"
      pattern: "bytes_stream|SseParser|push_bytes"
    - from: "agent-rs/src/main.rs"
      to: "agent-rs/src/tui.rs"
      via: "TokenChunk and tool lifecycle events"
      pattern: "TuiEvent::TokenChunk|ToolStart|ToolResult"
    - from: "agent-rs/src/tui/state.rs"
      to: "agent-rs/src/tui.rs"
      via: "collapsed tool timeline state read/write"
      pattern: "toggle_tool_collapsed|is_tool_collapsed"
---

<objective>
Adopt proven open-source streaming and TUI interaction patterns so Helix renders byte-level token output and rich tool widgets smoothly in terminal UX.

Purpose: Close the prototype-to-production UX gap for live output and readable tool context while preserving existing think-block semantics.
Output: Streaming refactor, richer tool timeline interactions, and regression tests for immediate rendering behavior.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/26-open-source-integration-references/26-RESEARCH.md
@agent-rs/src/main.rs
@agent-rs/src/stream.rs
@agent-rs/src/tui.rs
@agent-rs/src/tui/state.rs
@agent-rs/src/tui/api.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remove timer-batched token buffering and enforce byte-level streaming in TUI loop</name>
  <files>agent-rs/src/main.rs, agent-rs/src/stream.rs, agent-rs/tests/streaming_tui_refinement.rs</files>
  <behavior>
    - Streaming in TUI mode emits token chunks as soon as SSE bytes decode (no 30ms timer gate).
    - UTF-8 split chunks remain correctly reconstructed before rendering.
    - Non-stream fallback still triggers only for true stream decode failure + empty visible output.
  </behavior>
  <action>Refactor the TUI LLM loop to flush decoded visible content immediately on each SSE data event while keeping safety logic for decode recovery. Preserve existing tool-call aggregation behavior and avoid reintroducing line-buffered parsing.</action>
  <verify>
    <automated>cd agent-rs && cargo test -q --test streaming_tui_refinement</automated>
  </verify>
  <done>TUI streaming path is byte-driven and immediate, with passing tests covering chunking and fallback guards.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Upgrade TUI tool timeline widget behavior for readable live operations</name>
  <files>agent-rs/src/tui.rs, agent-rs/src/tui/state.rs, agent-rs/src/tui/api.rs, agent-rs/tests/streaming_tui_refinement.rs</files>
  <behavior>
    - Tool timeline entries show stable status progression (running, success, failed) with durations.
    - Collapsed/expanded tool entries preserve state across incremental stream updates.
    - Thinking/tool blocks stay readable and do not overwrite token stream content.
  </behavior>
  <action>Implement richer timeline rendering using existing state primitives and event channels; include keyboard-safe expand/collapse behavior and deterministic redraw ordering so tool updates do not flicker or hide assistant output.</action>
  <verify>
    <automated>cd agent-rs && cargo test -q --test streaming_tui_refinement</automated>
  </verify>
  <done>TUI tool widgets are interactively readable during live streaming with passing regression tests.</done>
</task>

<task type="auto">
  <name>Task 3: Run full Rust verification for streaming and TUI stability</name>
  <files>agent-rs/src/main.rs, agent-rs/src/tui.rs, agent-rs/src/tui/state.rs</files>
  <action>Execute full Rust test suite after streaming/widget changes to validate no regressions across chat, tool execution, and recovery logic.</action>
  <verify>
    <automated>cd agent-rs && cargo test -q</automated>
  </verify>
  <done>All Rust tests pass after Phase 26 plan-01 changes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SSE stream -> parser | Untrusted remote token bytes are parsed into visible UI content |
| parser -> TUI renderer | Render pipeline handles mixed token/tool/thinking events |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01 | D | `agent-rs/src/main.rs` streaming loop | mitigate | Remove timer batching while retaining fallback guard to prevent UI stall loops on decode errors. |
| T-26-02 | T | `agent-rs/src/tui.rs` timeline rendering | mitigate | Enforce deterministic event ordering and collapse-state preservation to avoid state corruption across redraws. |
| T-26-03 | I | streamed content surfaces | mitigate | Keep existing visible-content extraction and thinking-output sanitization path; do not render raw malformed chunks. |
</threat_model>

<verification>
- `cd agent-rs && cargo test -q --test streaming_tui_refinement` passes.
- `cd agent-rs && cargo test -q` passes.
</verification>

<success_criteria>
- STREAM-01 is satisfied with byte-level immediate token rendering in TUI mode.
- UX-01 remains readable through richer TUI tool/thinking block behavior under live streaming.
- No regressions in existing Rust runtime behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/26-open-source-integration-references/26-01-SUMMARY.md`
</output>
