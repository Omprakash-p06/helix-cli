---
wave: 1
depends_on: []
files_modified: ["agent-rs/Cargo.toml", "agent-rs/src/main.rs"]
autonomous: false
---

<objective>
Implement the foundational Terminal UI (TUI) for the orchestrator using ratatui, tokio, and crossterm, supporting basic input, output layout, and command preview.
</objective>

<task>
<description>Set up ratatui and tokio foundation in agent-rs</description>
<read_first>
- agent-rs/Cargo.toml
- agent-rs/src/main.rs
</read_first>
<action>
1. Add dependencies in `agent-rs/Cargo.toml`: `ratatui "0.26"`, `crossterm "0.27"`, `tokio { version = "1.0", features = ["full"] }`, `tui-input`.
2. Restructure `main.rs` to initialize a Ratatui `Terminal` with a `crossterm` backend and run an async main loop using `tokio`.
3. Set up the basic screen layout: a prompt area at the bottom, a main chat/output area in the middle, and a welcome banner at the top honoring the NO_COLOR fallback.
</action>
<acceptance_criteria>
- `agent-rs/Cargo.toml` contains `ratatui`
- `agent-rs/src/main.rs` initializes `Terminal` and `tokio::main`
- `cargo build` exits with 0 in `agent-rs`
</acceptance_criteria>
</task>

<task>
<description>Implement Input Layer with tui-input and Ghost Autocomplete</description>
<read_first>
- agent-rs/src/main.rs
</read_first>
<action>
1. Add a `tui-input` state object to track the user's input buffer.
2. Handle keyboard events from `crossterm` to update the input state.
3. Render ghost autocomplete suggestions (faintly beside the input) based on simple history or slash-command prefixes (e.g. `/help`).
4. Add a real-time token/character counter in the layout's status bar based on the input buffer length.
</action>
<acceptance_criteria>
- Terminal renders user input via `tui-input` widget
- Code contains event matching for `KeyCode::Char`
- Status bar component calculation includes string length function
</acceptance_criteria>
</task>

<task>
<description>Implement Multiline and Command Preview</description>
<read_first>
- agent-rs/src/main.rs
</read_first>
<action>
1. Enable `Enter` key behavior to add newlines for multiline input mode.
2. Define a submit shortcut (`Ctrl+Enter` or `Alt+Enter`).
3. Upon submission, render a temporary Command Preview panel via Ratatui `Popup` or `Clear` widget, waiting for user confirmation before executing actions.
</action>
<acceptance_criteria>
- Multiline input logic is mapped securely
- A predetermined action struct handles CommandPreview
- Popup rendering path invoked upon submission
</acceptance_criteria>
</task>
