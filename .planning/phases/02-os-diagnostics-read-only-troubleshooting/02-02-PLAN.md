---
phase: 02
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [agent-rs/src/agent_core/diagnostics/system.rs, agent-rs/src/security/policy.rs]
autonomous: true
requirements: [DIAG-02, DIAG-03, SEC-02]

must_haves:
  truths:
    - "Agent can list processes and resource usage using sysinfo"
    - "Agent can query service status (systemctl status on Linux, ServiceControl on Windows)"
    - "Agent can search for files using rg in diagnostic-relevant paths"
  artifacts:
    - path: "agent-rs/src/agent_core/diagnostics/system.rs"
      provides: "System state discovery and service introspection"
    - path: "agent-rs/src/security/policy.rs"
      provides: "Security whitelisting for system tools and diagnostic paths"
  key_links:
    - from: "agent-rs/src/agent_core/diagnostics/system.rs"
      to: "sysinfo"
      via: "Library calls"
    - from: "agent-rs/src/agent_core/diagnostics/system.rs"
      to: "systemctl status"
      via: "Service introspection (Linux)"
---

<objective>
Implement system state discovery tools (Processes, Services, Network) and safe file introspection using whitelisted diagnostic paths and tools like rg.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/02-os-diagnostics-read-only-troubleshooting/02-RESEARCH.md
@agent-rs/src/security/policy.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement System, Service, and Network Introspection</name>
  <files>agent-rs/src/agent_core/diagnostics/system.rs</files>
  <action>
    Implement `SystemProvider` using the `sysinfo` crate for processes and hardware stats.
    Implement service introspection:
    - Linux: Parse output of `systemctl status {service}`.
    - Windows: Use the `windows-service` crate to query service status.
    Ensure all data retrieval is read-only.
    Implement network interface discovery using the `network-interface` crate.
  </action>
  <verify>
    <automated>cargo test agent_core::diagnostics::system::tests</automated>
  </verify>
  <done>System, service, and network introspection logic is implemented and verified.</done>
</task>

<task type="auto">
  <name>Task 2: Implement File Search and diagnostic path whitelisting</name>
  <files>agent-rs/src/security/policy.rs, agent-rs/src/agent_core/tool_runtime.rs</files>
  <action>
    Implement `search_system_files` tool. Per Feedback 3, it MUST use `rg` (already in ALLOWLIST).
    Update `policy.rs` to support read-only access to essential diagnostic paths (e.g., `/etc`, `/var/log`, `/proc`) beyond the workspace root (per Feedback 4).
    Update `tier_allows_tool` for `ReadOnly` tier to include `list_processes`, `get_service_status`, and `search_system_files`.
    Ensure `search_system_files` implementation explicitly calls `rg` with appropriate flags (e.g., `--json`, `--max-count`).
  </action>
  <verify>
    <automated>cargo test security::policy::tests::test_diagnostic_path_allowlist</automated>
  </verify>
  <done>Diagnostic tools are whitelisted and file search is restricted to safe paths using rg.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Tool Execution -> Host System | Reading hardware/process/service state |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Information Disclosure | system-provider | mitigate | Limit process list detail (e.g., avoid environment variables/secrets in process args). |
| T-02-04 | Elevation of Privilege | search-tool | mitigate | Enforce specific path whitelisting in PolicyEngine to prevent reading sensitive files like /etc/shadow. |
</threat_model>

<verification>
1. Run `cargo test` to verify system stats and interface listing.
2. Check `PolicyEngine` tests for path traversal prevention and diagnostic path allowlist.
</verification>

<success_criteria>
- `list_processes` returns structured process data.
- `get_service_status` returns correct status for a known service (e.g., 'docker' or 'systemd-resolved').
- `search_system_files` succeeds for /etc/hostname using `rg` but fails for /etc/shadow.
</success_criteria>

<output>
After completion, create `.planning/phases/02-os-diagnostics-read-only-troubleshooting/02-02-SUMMARY.md`
</output>
