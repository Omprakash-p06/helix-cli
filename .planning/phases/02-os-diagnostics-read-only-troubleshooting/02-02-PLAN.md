---
phase: 02
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [agent-rs/src/agent_core/diagnostics/system.rs, agent-rs/src/security/policy.rs]
autonomous: true
requirements: [DIAG-02, DIAG-03, SEC-02]

must_haves:
  truths:
    - "Agent can list processes and resource usage using sysinfo"
    - "Agent can discover network interfaces"
    - "Agent can search for files in diagnostic-relevant paths"
  artifacts:
    - path: "agent-rs/src/agent_core/diagnostics/system.rs"
      provides: "System state discovery"
    - path: "agent-rs/src/security/policy.rs"
      provides: "Security whitelisting for system tools"
  key_links:
    - from: "agent-rs/src/agent_core/diagnostics/system.rs"
      to: "sysinfo"
      via: "Library calls"
    - from: "agent-rs/src/security/policy.rs"
      to: "get_system_stats"
      via: "tier_allows_tool whitelisting"
---

<objective>
Implement system state discovery tools (CPU, Memory, Processes, Network) and sandboxed file introspection capabilities.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/02-os-diagnostics-read-only-troubleshooting/02-RESEARCH.md
@agent-rs/src/security/policy.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement System and Network Introspection</name>
  <files>agent-rs/src/agent_core/diagnostics/system.rs</files>
  <action>
    Implement `SystemProvider` using the `sysinfo` crate (v0.30).
    Expose methods to list processes (with PID, name, CPU/MEM usage) and general system stats (Total Mem, Load Average).
    Implement network interface discovery using the `network-interface` crate.
    Ensure all data retrieval is read-only (no process termination or state modification).
  </action>
  <verify>
    <automated>cargo test test_system_introspection</automated>
  </verify>
  <done>System and network introspection logic is implemented and verified.</done>
</task>

<task type="auto">
  <name>Task 2: Implement File Search and whitelisting</name>
  <files>agent-rs/src/security/policy.rs, agent-rs/src/agent_core/tool_runtime.rs</files>
  <action>
    Implement `search_system_files` tool using `find` and `grep`/`rg` (restricted by PolicyEngine).
    Add `list_processes`, `list_network_interfaces`, and `search_system_files` to `tier_allows_tool` for `ReadOnly` tier.
    Ensure `get_system_stats` is properly wired to the new `SystemProvider`.
    Verify that `search_system_files` is restricted to read-only paths (e.g., /etc, /var/log, /tmp) via PolicyEngine's path validation logic or a specialized diagnostic allowlist.
  </action>
  <verify>
    <automated>cargo test test_diagnostic_tool_policy</automated>
  </verify>
  <done>Diagnostic tools are whitelisted and file search is restricted to safe paths.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Tool Execution -> Host System | Reading hardware/process state via sysinfo |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Information Disclosure | system-provider | mitigate | Limit process list detail (e.g., avoid environment variables/secrets in process args). |
| T-02-04 | Elevation of Privilege | search-tool | mitigate | Enforce path whitelisting in PolicyEngine to prevent reading sensitive files like /etc/shadow. |
</threat_model>

<verification>
1. Run `cargo test` to verify system stats and interface listing.
2. Check `PolicyEngine` tests for path traversal prevention in `search_system_files`.
</verification>

<success_criteria>
- `list_processes` returns structured process data.
- `search_system_files` succeeds for /etc/hostname but fails for /etc/shadow (simulated).
- `get_system_stats` reflects real-time memory/CPU usage.
</success_criteria>

<output>
After completion, create `.planning/phases/02-os-diagnostics-read-only-troubleshooting/02-02-SUMMARY.md`
</output>
