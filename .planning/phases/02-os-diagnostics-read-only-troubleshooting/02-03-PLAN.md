---
phase: 02
plan: 03
type: execute
wave: 2
depends_on: [02-01, 02-02]
files_modified: [agent-rs/src/agent_core/diagnostics/reasoning.rs, agent-rs/src/main.rs]
autonomous: true
requirements: [DIAG-01, DIAG-02, DIAG-03]

must_haves:
  truths:
    - "Agent follows a structured Observe -> Hypothesize -> Test loop"
    - "Diagnostic sessions are recorded in the audit log"
    - "Agent can synthesize a final diagnosis from collected evidence"
  artifacts:
    - path: "agent-rs/src/agent_core/diagnostics/reasoning.rs"
      provides: "Diagnostic Reasoning Loop (DRL) engine"
    - path: "agent-rs/src/agent_core/diagnostics/mod.rs"
      provides: "Unified diagnostics interface"
  key_links:
    - from: "agent-rs/src/agent_core/diagnostics/reasoning.rs"
      to: "agent-rs/src/agent_core/diagnostics/logs.rs"
      via: "Evidence collection"
    - from: "agent-rs/src/agent_core/diagnostics/reasoning.rs"
      to: "agent-rs/src/agent_core/diagnostics/system.rs"
      via: "Hypothesis testing"
---

<objective>
Implement the Diagnostic Reasoning Loop (DRL) to enable autonomous troubleshooting and perform end-to-end validation of Phase 02 requirements.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/02-os-diagnostics-read-only-troubleshooting/02-RESEARCH.md
@.planning/phases/02-os-diagnostics-read-only-troubleshooting/02-01-SUMMARY.md
@.planning/phases/02-os-diagnostics-read-only-troubleshooting/02-02-SUMMARY.md
@.planning/phases/02-os-diagnostics-read-only-troubleshooting/02-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Diagnostic Reasoning Loop (DRL) Engine</name>
  <files>agent-rs/src/agent_core/diagnostics/reasoning.rs</files>
  <action>
    Implement a `DiagnosticEngine` that manages the troubleshooting lifecycle.
    Define a state machine for "Observe -> Hypothesize -> Test -> Synthesize".
    Implement logic to format evidence from `LogProvider` and `SystemProvider` for the LLM.
    Add methods to track "proven" vs "disproven" hypotheses in the session context.
    Ensure every transition in the state machine is logged to the audit log.
  </action>
  <verify>
    <automated>cargo test agent_core::diagnostics::reasoning::tests</automated>
  </verify>
  <done>DRL engine is implemented and manages diagnostic state correctly.</done>
</task>

<task type="auto">
  <name>Task 2: Integration Testing and Final Validation</name>
  <files>agent-rs/src/main.rs, agent-rs/tests/diagnostic_validation.rs</files>
  <action>
    Integrate the `DiagnosticEngine` into the main agent loop.
    Create an integration test in `agent-rs/tests/diagnostic_validation.rs` that simulates complex failure scenarios:
    - "Service crash with log evidence": Verify agent reads logs, finds error, checks service status.
    - "Resource exhaustion": Verify agent checks CPU/Mem and identifies high-usage process.
    - "Path permission check": Verify `rg` works on `/etc` but is blocked on `/root` or `/etc/shadow`.
    Ensure all tests in `02-VALIDATION.md` pass.
  </action>
  <verify>
    <automated>cargo test --test diagnostic_validation</automated>
  </verify>
  <done>Full diagnostic loop is functional and verified via end-to-end simulation against 02-VALIDATION.md.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Reasoning Loop -> Audit Log | Recording high-level logic and tool outcomes |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Repudiation | audit-logger | mitigate | Ensure every step of the DRL (Observation, Hypothesis, Test result) is recorded in the immutable audit chain. |
</threat_model>

<verification>
1. Run `cargo test --test diagnostic_validation` to see the full loop in action.
2. Inspect `agent.log` or audit records to ensure "Observe", "Hypothesize", and "Test" stages are clearly demarcated.
</verification>

<success_criteria>
- Agent successfully identifies a simulated "Service Down" issue using `list_processes` and `get_system_logs`.
- Every tool call made during the reasoning loop is recorded in the immutable audit log.
- All mapping in `02-VALIDATION.md` is confirmed via automated tests.
</success_criteria>

<output>
After completion, create `.planning/phases/02-os-diagnostics-read-only-troubleshooting/02-03-SUMMARY.md`
</output>
