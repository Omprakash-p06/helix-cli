---
phase: 02
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [agent-rs/src/agent_core/diagnostics/logs.rs, agent-rs/src/security/policy.rs]
autonomous: true
requirements: [DIAG-01, SEC-02]

must_haves:
  truths:
    - "Agent can retrieve Linux system logs via journalctl"
    - "Agent can parse Windows .evtx log files"
    - "Log tools are restricted to ReadOnly tier in PolicyEngine"
  artifacts:
    - path: "agent-rs/src/agent_core/diagnostics/logs.rs"
      provides: "Cross-platform log retrieval"
    - path: "agent-rs/src/security/policy.rs"
      provides: "Security whitelisting for log tools"
  key_links:
    - from: "agent-rs/src/agent_core/diagnostics/logs.rs"
      to: "journalctl"
      via: "Command execution"
    - from: "agent-rs/src/security/policy.rs"
      to: "get_system_logs"
      via: "tier_allows_tool whitelisting"
---

<objective>
Implement cross-platform log retrieval and parsing tools, ensuring safe read-only access to system logs on both Linux and Windows.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/02-os-diagnostics-read-only-troubleshooting/02-RESEARCH.md
@agent-rs/src/security/policy.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement LogProvider and platform-specific logic</name>
  <files>agent-rs/src/agent_core/diagnostics/logs.rs, agent-rs/src/agent_core/diagnostics/mod.rs</files>
  <action>
    Create a `LogProvider` trait with a `get_logs` method.
    Implement it for Linux using `journalctl -o json -n {limit}`.
    Implement it for Windows using the `evtx` crate to parse standard event log paths (e.g., System, Application).
    Ensure the implementation handles JSON parsing of journalctl output.
    Add `diagnostics` module to `agent-rs/src/agent_core/mod.rs`.
  </action>
  <verify>
    <automated>cargo test test_log_parsing</automated>
  </verify>
  <done>Log retrieval logic is implemented and passes unit tests on the host platform.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate log tools into PolicyEngine</name>
  <files>agent-rs/src/security/policy.rs, agent-rs/src/agent_core/tool_runtime.rs</files>
  <action>
    Add `get_system_logs` to `tier_allows_tool` for `PermissionTier::ReadOnly`.
    Update the `ALLOWLIST` in `policy.rs` to include `journalctl`.
    Implement the `get_system_logs` tool handler in `tool_runtime.rs` (or equivalent tool dispatcher) that calls the `LogProvider`.
    Add a test case to `policy.rs` verifying that `get_system_logs` is allowed in ReadOnly tier while mutation tools remain blocked.
  </action>
  <verify>
    <automated>cargo test security::policy::tests</automated>
  </verify>
  <done>Log tools are whitelisted and security enforcement is verified via tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Orchestrator -> System Logs | Untrusted host data ingested into agent reasoning context |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | log-reader | mitigate | Use `journalctl -o json` to prevent escape character injection in parsing. |
| T-02-02 | Info Disclosure | policy-engine | mitigate | Enforce ReadOnly tier for log access; limit line count to prevent context flooding. |
</threat_model>

<verification>
1. Run `cargo test` to ensure log parsing and policy logic are sound.
2. Manually verify `journalctl` command generation in a simulated environment if possible.
</verification>

<success_criteria>
- `get_system_logs` tool returns structured logs.
- PolicyEngine permits `get_system_logs` in ReadOnly tier.
- PolicyEngine blocks any command-chaining or metacharacters in log tool arguments.
</success_criteria>

<output>
After completion, create `.planning/phases/02-os-diagnostics-read-only-troubleshooting/02-01-SUMMARY.md`
</output>
