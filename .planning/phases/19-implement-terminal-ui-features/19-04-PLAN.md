---
phase: 19-implement-terminal-ui-features
plan: 04
type: execute
wave: 4
depends_on:
  - 19-03
files_modified:
  - agent-rs/src/main.rs
  - agent-rs/src/tui.rs
  - agent-rs/src/tui/events.rs
autonomous: true
requirements:
  - TUI-05
user_setup: []

must_haves:
  truths:
    - "The TUI shows a typing indicator, tool spinner, and top error banner instead of looking frozen while waiting for output."
    - "Interrupting generation preserves partial output, and idle quitting still requires an explicit second `Ctrl+C`."
    - "The finalized implementation passes formatting, tests, and clippy with warnings treated as errors."
  artifacts:
    - path: "agent-rs/src/tui.rs"
      provides: "Interactive polish, animation ticks, and regression tests"
      min_lines: 550
    - path: "agent-rs/src/main.rs"
      provides: "Exhaustive TUI action handling with no unreachable fallback"
      contains: "match action_rx.recv().await"
    - path: "agent-rs/src/tui/events.rs"
      provides: "Focused key handling for paging, banner dismiss, and palette interactions"
      min_lines: 80
  key_links:
    - from: "agent-rs/src/tui.rs"
      to: "agent-rs/src/main.rs"
      via: "interrupt and quit action handling"
      pattern: "TuiAction::Interrupt|TuiAction::Quit"
    - from: "agent-rs/src/tui.rs"
      to: "agent-rs/src/tui/events.rs"
      via: "Esc dismissal and paging commands"
      pattern: "Esc|ScrollPageUp|ScrollPageDown"
---

<objective>
Add the final UX polish and verification gate: visible progress states, cleaner interaction rules, regression tests, and a warning-free Rust build.

Purpose: Plan 04 closes the remaining quality gap so the redesigned TUI feels responsive and shippable instead of merely feature-present.
Output: Polished feedback behaviors, exhaustive action handling, and passing `fmt`, `test`, and `clippy` runs.
</objective>

<execution_context>
@/home/omprakash/.codex/get-shit-done/workflows/execute-plan.md
@/home/omprakash/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-implement-terminal-ui-features/REQUIREMENTS.md
@.planning/phases/14-fix-tui-missing-output-bug/14-02-SUMMARY.md
@agent-rs/src/main.rs
@agent-rs/src/tui.rs
@agent-rs/src/tui/events.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add visible progress, interrupt, and navigation polish</name>
  <files>agent-rs/src/main.rs, agent-rs/src/tui.rs, agent-rs/src/tui/events.rs</files>
  <read_first>
    - .planning/phases/19-implement-terminal-ui-features/REQUIREMENTS.md
    - .planning/phases/14-fix-tui-missing-output-bug/14-02-SUMMARY.md
    - agent-rs/src/main.rs
    - agent-rs/src/tui.rs
    - agent-rs/src/tui/events.rs
  </read_first>
  <acceptance_criteria>
    - Before the first visible token, the renderer shows a three-dot typing indicator.
    - While tools are running, spinner frames rotate through the exact sequence `◐`, `◓`, `◑`, `◒`.
    - `Esc` dismisses an active top-of-screen `StatusBanner`.
    - `Ctrl+U` and `Ctrl+D` perform half-page scrolling without changing the input buffer.
    - Idle quitting requires a second `Ctrl+C`, while generating still maps a single `Ctrl+C` to `TuiAction::Interrupt`.
  </acceptance_criteria>
  <action>Finish the interactive polish in `agent-rs/src/tui.rs` and `agent-rs/src/tui/events.rs`. Use the existing generation/heartbeat states to render a three-dot typing indicator before the first visible token and drive tool-progress spinners with a lightweight tick that only repaints spinner/banner state; do not reintroduce buffered token rendering that would violate the live-streaming work from Phases 14 and 16. Surface `StatusBanner` at the top edge of the conversation area, dismissible with `Esc`. Add `Ctrl+U` and `Ctrl+D` half-page scrolling plus existing `PageUp`/`PageDown` support. Change the idle quit path so the first `Ctrl+C` sets a short-lived `Press Ctrl+C again to quit` banner and the second exits; keep the generating path mapped to `Interrupt` so partial output still flushes through the existing interrupt-safe logic in `main.rs`. In `main.rs`, remove the current unreachable `Some(_)` fallback and handle the final `TuiAction` surface explicitly.</action>
  <verify>
    <automated>cd agent-rs && cargo test -q tui::tests:: main::tests:: -- --nocapture</automated>
  </verify>
  <done>The TUI no longer looks frozen during TTFT/tool work, interaction rules are precise, and `main.rs` handles the finalized action surface without unreachable match arms.</done>
</task>

<task type="auto">
  <name>Task 2: Add regression coverage and enforce a warning-free Rust build</name>
  <files>agent-rs/src/main.rs, agent-rs/src/tui.rs, agent-rs/src/tui/events.rs</files>
  <read_first>
    - agent-rs/src/main.rs
    - agent-rs/src/tui.rs
    - agent-rs/src/tui/events.rs
  </read_first>
  <acceptance_criteria>
    - The test suite covers layout stability, token bar rendering, palette filtering, markdown/diff classification, tool collapse toggling, pending-message indicators, and interrupt partial flush behavior.
    - `cargo fmt --check` passes.
    - `cargo test -q` passes.
    - `cargo clippy -- -D warnings` passes.
  </acceptance_criteria>
  <action>Add focused regression tests in the existing Rust test modules instead of creating broad snapshot fixtures that are hard to maintain. Cover at least these concrete behaviors: layout split stays within bounds for wide and compact modes, token usage bar renders the correct filled/empty counts, palette filtering matches example text, diff lines receive green/red classification, `Enter` toggles a tool block's collapsed state, scrolling up increments `pending_new_messages` instead of resetting the viewport, and interrupt handling preserves partial output. After the tests are in place, run `cargo fmt --check`, `cargo test -q`, and `cargo clippy -- -D warnings`, fixing any warnings or dead code introduced by the new TUI modules before closing the plan.</action>
  <verify>
    <automated>cd agent-rs && cargo fmt --check && cargo test -q && cargo clippy -- -D warnings</automated>
  </verify>
  <done>The redesigned TUI is backed by regression tests for its tricky states and the crate is clean under fmt, test, and clippy with warnings elevated to errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input -> interrupt/quit controls | Fast repeated control keys can cancel generation or terminate the app. |
| background progress tick -> renderer | Timer-driven animation updates must not starve or corrupt live token rendering. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-10 | D | spinner/typing updates | mitigate | Keep progress animation on a lightweight repaint tick only and preserve immediate token event handling so animation does not throttle or buffer streamed content. |
| T-19-11 | R | interrupt/quit controls | mitigate | Distinguish generating vs idle `Ctrl+C` paths explicitly in code and back them with tests so partial output flush and double-quit semantics are auditable. |
| T-19-12 | T | warning-free finalization | mitigate | Gate completion on `cargo clippy -- -D warnings` and remove unreachable or dead branches so the final action/event surface cannot silently diverge. |
</threat_model>

<verification>
Run `cd agent-rs && cargo test -q tui::tests:: main::tests:: -- --nocapture` after Task 1. Run `cd agent-rs && cargo fmt --check && cargo test -q && cargo clippy -- -D warnings` after Task 2. Do not mark the plan complete if clippy still reports even a single warning.
</verification>

<success_criteria>
Plan 19-04 is complete when progress states are visibly rendered, interrupt and quit behavior are explicit and tested, and the Rust crate is clean under fmt, test, and clippy with warnings denied.
</success_criteria>

<output>
After completion, create `.planning/phases/19-implement-terminal-ui-features/19-04-SUMMARY.md`
</output>
