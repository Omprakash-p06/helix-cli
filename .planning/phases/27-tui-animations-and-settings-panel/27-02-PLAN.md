---
phase: 27-tui-animations-and-settings-panel
plan: 02
type: execute
wave: 2
depends_on: ["27-01"]
files_modified: [agent-rs/src/tui.rs, agent-rs/src/tui/state.rs, agent-rs/src/tui/events.rs]
autonomous: false
requirements: [TUI-10]
must_haves:
  truths:
    - "Pressing Ctrl+S opens the settings modal"
    - "User can navigate between settings items using arrow keys"
    - "Changing a theme in settings updates the UI immediately"
    - "Model and Mode changes are persisted for the session"
  artifacts:
    - path: "agent-rs/src/tui/state.rs"
      provides: "SettingsState with model, mode, theme, and permissions"
  key_links:
    - from: "SettingsState"
      to: "AppConfig"
      via: "Session override"
---

<objective>
Implement a persistent settings modal for user configuration of models, execution modes, themes, and permission tiers.

Purpose: Provide user-facing control over agent behavior without restarting the process.
Output: A functional List-Detail settings modal and configuration persistence logic.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/27-tui-animations-and-settings-panel/27-RESEARCH.md
@agent-rs/src/tui.rs
@agent-rs/src/tui/state.rs
@agent-rs/src/tui/events.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Settings Modal UI and State</name>
  <files>agent-rs/src/tui/state.rs, agent-rs/src/tui.rs</files>
  <action>
    - Define `SettingsState` in `agent-rs/src/tui/state.rs`:
      - Fields for `selected_item`, `active_modal` (bool), and values for (Model, Mode, Theme, Sidebar, Permissions).
    - Implement `draw_settings_modal` in `agent-rs/src/tui.rs`:
      - Use a centered `Rect` (60% width, 40% height).
      - Use a `Layout` to split into Left (List of categories) and Right (Active setting details/options).
      - Use `Clear` to ensure the modal is opaque over the chat history.
    - Add `TuiAction::OpenSettings` and `TuiAction::CloseSettings`.
  </action>
  <verify>
    <automated>cargo check -p agent-rs</automated>
  </verify>
  <done>Settings modal UI structure is implemented and compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Settings Event Handling and Navigation</name>
  <files>agent-rs/src/tui/events.rs, agent-rs/src/tui.rs</files>
  <action>
    - Update `handle_key_event` in `agent-rs/src/tui.rs`:
      - Map `Ctrl+S` to `TuiAction::OpenSettings`.
      - When in `InputMode::Settings`, intercept Up/Down for category selection and Left/Right or Enter for value selection.
      - Map `Esc` to `TuiAction::CloseSettings`.
    - Implement navigation logic:
      - Categories: [General, Interface, Security].
      - General: Model Name (Text), Execution Mode (Toggle: Chat/Agentic).
      - Interface: Theme (Select), Sidebar Visibility (Toggle).
      - Security: Permission Tier (Select: ReadOnly, WorkspaceWrite, FullExec).
  </action>
  <verify>
    <automated>cargo check -p agent-rs</automated>
  </verify>
  <done>Users can open, navigate, and modify settings via keyboard input.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Settings Modal Interaction</name>
  <what-built>Settings Modal Interaction</what-built>
  <how-to-verify>
    1. Launch the agent TUI.
    2. Press `Ctrl+S` to open the settings modal.
    3. Use arrow keys to navigate between categories.
    4. Change the Theme and verify the UI updates immediately.
    5. Change the Execution Mode and verify the status bar updates.
    6. Press `Esc` to close the modal.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
  <action>Human verification of settings modal navigation and theme switching.</action>
  <verify>Manual verification by the user.</verify>
  <done>User approves the settings modal interaction.</done>
</task>

<task type="auto">
  <name>Task 3: Integration and Persistence</name>
  <files>agent-rs/src/tui.rs, agent-rs/src/tui/state.rs</files>
  <action>
    - Ensure `SettingsState` values are synced with `TuiApp` fields (e.g., `model_name`, `exec_mode_label`).
    - Implement session-based persistence:
      - When a setting is changed, send a `TuiAction` that the orchestrator can use to update its internal state or reload the model if necessary.
    - Add a unit test `test_settings_toggle` in `agent-rs/src/tui/state.rs` to verify that changing a setting correctly updates the state.
  </action>
  <verify>
    <automated>cargo test test_settings_toggle</automated>
  </verify>
  <done>Settings are fully integrated and functional with unit test coverage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Settings → Orchestrator | Settings changes affect core agent behavior (model, permissions). |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-02-01 | E (Elevation of Privilege) | Permissions Setting | mitigate | Ensure the orchestrator validates the requested permission tier against allowed bounds. |
</threat_model>

<verification>
1. Open settings (`Ctrl+S`).
2. Navigate to "Security".
3. Change Permission Tier.
4. Close settings and execute a tool that requires higher permissions; verify the agent respects the new setting.
</verification>

<success_criteria>
- Settings modal is accessible and navigable.
- Theme changes take effect immediately.
- Execution mode and model changes update the session state.
- Security permissions are adjustable via UI.
</success_criteria>

<output>
After completion, create `.planning/phases/27-tui-animations-and-settings-panel/27-02-SUMMARY.md`
</output>
