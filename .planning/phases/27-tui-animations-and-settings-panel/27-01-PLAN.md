---
phase: 27-tui-animations-and-settings-panel
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [agent-rs/Cargo.toml, agent-rs/src/tui.rs, agent-rs/src/tui/state.rs]
autonomous: true
requirements: [TUI-06, TUI-07, TUI-08, TUI-09]
must_haves:
  truths:
    - "TUI redraws periodically at ~30 FPS even with no user input"
    - "A visible spinner appears while the model is in 'thinking' state"
    - "Active tool executions in the sidebar pulse with a color effect"
    - "Tool completion triggers a green/red flash effect based on success/failure (per TUI-08)"
    - "New chat messages fade in smoothly"
  artifacts:
    - path: "agent-rs/src/tui.rs"
      provides: "Hybrid event loop with tick interval"
    - path: "agent-rs/src/tui/state.rs"
      provides: "Animation state tracking (Throbber, EffectManager)"
  key_links:
    - from: "tokio::time::interval"
      to: "terminal.draw"
      via: "tick event"
---

<objective>
Upgrade the TUI event loop to support high-performance animations and implement core visual effects: thinking spinner, pulsing tool status, completion flashes, and message fade-ins.

Purpose: Improve the perceived responsiveness and polish of the terminal interface.
Output: A hybrid tick/event loop and integrated animation widgets.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/27-tui-animations-and-settings-panel/27-RESEARCH.md
@agent-rs/src/tui.rs
@agent-rs/src/tui/state.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Animation Dependencies and Refactor Event Loop</name>
  <files>agent-rs/Cargo.toml, agent-rs/src/tui.rs</files>
  <behavior>
    - Ensure that the TUI loop triggers a redraw at least once every 33ms when no other events are pending.
    - Test case: Verify `dt` (delta time) is correctly calculated between ticks.
  </behavior>
  <action>
    - Add `tachyonfx = "0.11.1"` and `throbber-widgets-tui = "0.11.0"` to agent-rs/Cargo.toml.
    - Refactor `run_tui` in `agent-rs/src/tui.rs` to replace the 50ms polling loop with a hybrid loop:
      - Use `tokio::time::interval(Duration::from_millis(33))` for ~30 FPS ticks.
      - Use `tokio::select!` to handle interval ticks, crossterm events (via `spawn_blocking`), and orchestrator events.
      - Track `last_tick: Instant` to calculate `dt: Duration` (delta time) on each frame.
      - Pass `dt` to the `draw` function and `TuiApp::on_tick`.
  </action>
  <verify>
    <automated>cargo check -p agent-rs</automated>
  </verify>
  <done>Hybrid event loop implemented and codebase compiles with new dependencies.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Thinking Spinner and Tool Execution Effects</name>
  <files>agent-rs/src/tui.rs, agent-rs/src/tui/state.rs</files>
  <behavior>
    - Verify `throbber_state` increments on `on_tick`.
    - Test `test_throbber_tick`: Check that `throbber_state` index changes after calling `calc_next`.
    - Verify `effect_manager` receives `dt` and updates internal state.
    - Verify tool completion triggers flash effects (green for success, red for failure).
  </behavior>
  <action>
    - Add `throbber_state: ThrobberState` to `TuiApp` in `agent-rs/src/tui/state.rs`.
    - Add `effect_manager: tachyonfx::EffectManager` to `TuiApp`.
    - In `TuiApp::on_tick(dt: Duration)`:
      - Call `throbber_state.calc_next()` if `is_generating` is true and `streaming_content` is empty.
      - Call `effect_manager.update(dt)`.
    - In `draw_chat_area` and `draw_status_bar`:
      - Render the `Throbber` widget when the model is in the "thinking" state.
    - In `draw_sidebar` (Tool Timeline section):
      - Use `tachyonfx` to apply a pulsing effect (`fx::ping_pong` + `fx::fade_to_fg`) to `Running` tool entries (per TUI-08).
      - Trigger a green flash (`fx::flash(Color::Green)`) when a tool completes successfully.
      - Trigger a red flash (`fx::flash(Color::Red)`) when a tool fails or times out.
    - Add unit test `test_throbber_tick` in `agent-rs/src/tui/state.rs`.
  </action>
  <verify>
    <automated>cargo test -p agent-rs tui::state::tests::test_throbber_tick</automated>
  </verify>
  <done>Animations for thinking state and tool execution (including completion flashes per TUI-08) are visible and update smoothly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement Message Fade-in and Token Gauge Animation</name>
  <files>agent-rs/src/tui.rs, agent-rs/src/tui/state.rs</files>
  <behavior>
    - Test `test_gauge_interpolation`: `animated_tokens` should smoothly approach `current_tokens` over multiple ticks.
    - Example: target=100, current=0 -> tick -> current > 0.
  </behavior>
  <action>
    - In `TuiApp`:
      - Add `animated_tokens: f32` for smooth gauge updates.
      - Update `on_tick` to interpolate `animated_tokens` toward `current_tokens` using damping: `delta = (target - current) * 0.2`.
    - Implement `TUI-07` (Fade-in effect for new messages):
      - When a new `ChatEntry` is added, register a `tachyonfx::fx::fade_from` effect for its estimated area.
      - Use a duration of 150ms as per requirements.
    - Update `draw_sidebar` and `draw_input_area` to use `animated_tokens` for the gauge bar display.
    - Add unit test `test_gauge_interpolation` in `agent-rs/src/tui/state.rs`.
  </action>
  <verify>
    <automated>cargo test -p agent-rs tui::state::tests::test_gauge_interpolation</automated>
  </verify>
  <done>New messages fade in and the token gauge animates smoothly with automated test coverage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Event Loop → UI | High-frequency redraws can consume excessive CPU if not throttled. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-01-01 | S (Denial of Service) | run_tui loop | mitigate | Limit tick rate to 30 FPS and ensure `draw` is efficient. |
</threat_model>

<verification>
1. Run `cargo run -p agent-rs` and verify the TUI boots.
2. Ask a question and verify the spinner appears during the "Thinking" phase.
3. Observe tool execution in the sidebar and verify the pulsing effect.
4. Execute a tool (e.g., shell command) and verify the green flash on success or red flash on failure.
5. Verify the token gauge bar moves smoothly rather than jumping.
</verification>

<success_criteria>
- Event loop runs at a steady ~30 FPS.
- Throbber spinner is functional during latency/thinking.
- Tool execution status pulses clearly.
- Tool completion triggers a green/red flash per TUI-08.
- Token gauge updates are visually interpolated.
</success_criteria>

<output>
After completion, create `.planning/phases/27-tui-animations-and-settings-panel/27-01-SUMMARY.md`
</output>
