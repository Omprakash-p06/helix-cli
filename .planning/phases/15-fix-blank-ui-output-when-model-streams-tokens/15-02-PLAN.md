---
phase: 15-fix-blank-ui-output-when-model-streams-tokens
plan: 02
type: execute
wave: 2
depends_on:
  - 15-01
files_modified:
  - agent-rs/src/utils.rs
  - agent-rs/src/main.rs
  - agent-rs/src/server.rs
  - agent-rs/src/tui.rs
autonomous: true
requirements: [CHAT-03, CHAT-04]
must_haves:
  truths:
    - "Chat output removes only clear artifacts and preserves semantic/user-authored structures."
    - "Consecutive identical sentence duplication is removed without aggressive rewriting."
    - "Code fences, inline code, and tool JSON payloads remain unchanged after cleanup."
  artifacts:
    - path: "agent-rs/src/utils.rs"
      provides: "Protected-block extraction/restore + dedup + quote normalization + clean_chat_output pipeline"
    - path: "agent-rs/src/main.rs"
      provides: "Chat-path integration of full output cleaner"
    - path: "agent-rs/src/server.rs"
      provides: "Web/SSE chat-path integration of same cleaner"
  key_links:
    - from: "agent-rs/src/utils.rs"
      to: "agent-rs/src/main.rs"
      via: "clean_chat_output invocation in chat-mode render/store path"
      pattern: "clean_chat_output"
    - from: "agent-rs/src/utils.rs"
      to: "agent-rs/src/server.rs"
      via: "clean_chat_output invocation before outbound text events"
      pattern: "clean_chat_output"
---

<objective>
Complete chat-output polish with conservative dedup and quote normalization while preserving protected content structures.

Purpose: Implement CHAT-03 and CHAT-04 exactly as constrained in phase context decisions.
Output: End-to-end clean chat output pipeline with preservation guardrails and tests.
</objective>

<execution_context>
@$HOME/.copilot/get-shit-done/workflows/execute-plan.md
@$HOME/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-fix-blank-ui-output-when-model-streams-tokens/15-CONTEXT.md
@agent-rs/src/utils.rs
@agent-rs/src/main.rs
@agent-rs/src/server.rs
@agent-rs/src/tui.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build protected-block aware cleanup utilities</name>
  <read_first>agent-rs/src/utils.rs, agent-rs/src/tools.rs, .planning/phases/15-fix-blank-ui-output-when-model-streams-tokens/15-CONTEXT.md</read_first>
  <files>agent-rs/src/utils.rs</files>
  <behavior>
    - Test 1: `"Hello. Hello. How are you?"` becomes `"Hello. How are you?"`.
    - Test 2: Curly quotes are normalized to straight quotes.
    - Test 3: Fenced code blocks, inline code, and tool-shaped JSON payloads are byte-preserved.
  </behavior>
  <action>Extend `agent-rs/src/utils.rs` with: (1) protected-block extraction/restore helpers for fenced code blocks, inline code spans, and tool-shaped JSON payloads; (2) `deduplicate_consecutive_sentences(text: &str) -> String` using exact consecutive sentence match (case-sensitive, trim trailing spaces only); (3) `normalize_quotes(text: &str) -> String` for curly quote normalization plus unambiguous closing-quote repair; and (4) `clean_chat_output(text: &str) -> String` pipeline order locked as protect -> strip_reasoning_blocks -> dedup -> normalize -> restore (D-15). Include unit tests for each rule and order-sensitive behavior.</action>
  <acceptance_criteria>
    - `agent-rs/src/utils.rs` exports `clean_chat_output`, `deduplicate_consecutive_sentences`, and `normalize_quotes`.
    - Utility tests confirm protected blocks are restored unchanged.
    - Utility tests confirm dedup is consecutive-exact and does not perform global rewriting.
    - `cd agent-rs && cargo test -q` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd agent-rs && cargo test -q</automated>
  </verify>
  <done>A deterministic conservative cleaner exists with hard preservation guardrails.</done>
</task>

<task type="auto">
  <name>Task 2: Wire full cleaner into chat-mode output flow and validate non-regression</name>
  <read_first>agent-rs/src/main.rs, agent-rs/src/server.rs, agent-rs/src/tui.rs, agent-rs/src/utils.rs</read_first>
  <files>agent-rs/src/main.rs, agent-rs/src/server.rs, agent-rs/src/tui.rs</files>
  <action>Replace chat-mode direct reasoning-strip calls with `clean_chat_output` in all chat-facing paths that finalize visible assistant text for terminal/TUI/web. Ensure wiring remains chat-only and does not mutate agentic outputs (D-02, D-07..D-14). Keep streaming behavior intact (no phase-16 byte-stream refactor here). Add or update integration-level tests validating code block and tool JSON preservation after end-to-end cleaning.
</action>
  <acceptance_criteria>
    - `agent-rs/src/main.rs` invokes `clean_chat_output` in chat output finalization path.
    - `agent-rs/src/server.rs` invokes `clean_chat_output` for chat-mode outbound text events.
    - `agent-rs/src/tui.rs` does not add additional destructive text transforms that violate protected-block guarantees.
    - `cd agent-rs && cargo check -q && cargo test -q` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd agent-rs && cargo check -q && cargo test -q</automated>
  </verify>
  <done>Chat-mode responses are polished and conservative with protected content preserved and no agentic regression.</done>
</task>

</tasks>

<verification>
- `cd agent-rs && cargo check -q && cargo test -q`
- Manual chat-mode checks:
  - `hey!` returns concise clean response (no reasoning traces)
  - duplicated sentence sample is collapsed conservatively
  - fenced code sample remains unchanged
</verification>

<success_criteria>
- CHAT-03 covered by exact consecutive-sentence dedup in chat-mode output.
- CHAT-04 covered by quote normalization with protected-structure preservation.
- Cleaner integration is chat-only and does not alter agentic transparency behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/15-fix-blank-ui-output-when-model-streams-tokens/15-02-SUMMARY.md`
</output>
