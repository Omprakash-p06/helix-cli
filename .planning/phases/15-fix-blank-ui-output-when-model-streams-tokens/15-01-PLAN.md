---
phase: 15-fix-blank-ui-output-when-model-streams-tokens
plan: 01
type: execute
wave: 1
depends_on:
  - 14-02
files_modified:
  - agent-rs/src/config.rs
  - scripts/config.py
  - agent-rs/src/main.rs
  - agent-rs/src/server.rs
  - agent-rs/src/utils.rs
autonomous: true
requirements: [CHAT-01, CHAT-02]
must_haves:
  truths:
    - "Chat mode responses never expose internal reasoning tags or reasoning content."
    - "Agentic mode keeps existing reasoning/transparency behavior."
    - "Chat-mode system prompt is explicitly separate from agentic prompt behavior."
  artifacts:
    - path: "agent-rs/src/utils.rs"
      provides: "Reasoning-block stripping utilities for chat-mode sanitization"
    - path: "agent-rs/src/config.rs"
      provides: "Prompt configuration fields loaded from Python config bridge"
    - path: "agent-rs/src/main.rs"
      provides: "Mode-aware prompt selection and chat-path sanitization wiring"
  key_links:
    - from: "scripts/config.py"
      to: "agent-rs/src/config.rs"
      via: "Python config bridge fields for prompt text"
      pattern: "CHAT_SYSTEM_PROMPT|AGENTIC_SYSTEM_PROMPT"
    - from: "agent-rs/src/main.rs"
      to: "agent-rs/src/utils.rs"
      via: "chat-only call path before rendering/storage"
      pattern: "strip_reasoning_blocks"
---

<objective>
Define strict chat-mode prompt and reasoning filtering so chat output is concise/professional while preserving existing agentic transparency behavior.

Purpose: Implement the phase boundary for CHAT-01 and CHAT-02 with no scope bleed into streaming/tool phases.
Output: Prompt split + chat-only reasoning-strip pipeline wired into terminal and web response paths.
</objective>

<execution_context>
@$HOME/.copilot/get-shit-done/workflows/execute-plan.md
@$HOME/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-fix-blank-ui-output-when-model-streams-tokens/15-CONTEXT.md
@.planning/phases/15-fix-blank-ui-output-when-model-streams-tokens/15-RESEARCH.md
@.planning/research/SUMMARY.md
@agent-rs/src/main.rs
@agent-rs/src/server.rs
@agent-rs/src/config.rs
@scripts/config.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Introduce explicit chat vs agentic prompt configuration and wiring</name>
  <read_first>agent-rs/src/config.rs, scripts/config.py, agent-rs/src/main.rs, .planning/phases/15-fix-blank-ui-output-when-model-streams-tokens/15-CONTEXT.md</read_first>
  <files>agent-rs/src/config.rs, scripts/config.py, agent-rs/src/main.rs</files>
  <behavior>
    - Test 1: Chat mode builds requests with a dedicated concise chat prompt.
    - Test 2: Agentic mode uses existing agentic prompt behavior and tool grammar flow unchanged.
  </behavior>
  <action>Add `chat_system_prompt` and `agentic_system_prompt` fields to `AppConfig` bridge in `agent-rs/src/config.rs` and expose defaults from `scripts/config.py` via constants (`CHAT_SYSTEM_PROMPT`, `AGENTIC_SYSTEM_PROMPT`). In `agent-rs/src/main.rs`, replace hardcoded prompt branching with config-driven mode routing so chat mode always gets the concise no-reasoning prompt while agentic mode keeps existing persona/tool guidance semantics (per D-01/D-02, D-16). Do not change tool payload gating behavior for agentic mode.</action>
  <acceptance_criteria>
    - `scripts/config.py` contains both `CHAT_SYSTEM_PROMPT` and `AGENTIC_SYSTEM_PROMPT` constants.
    - `agent-rs/src/config.rs` JSON bridge includes both prompt fields in `AppConfig`.
    - `agent-rs/src/main.rs` prompt selection reads from `app_config.chat_system_prompt` in chat mode.
    - `cd agent-rs && cargo check -q` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd agent-rs && cargo check -q</automated>
  </verify>
  <done>Prompt behavior is mode-explicit and no longer relies on an empty chat system prompt fallback.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add chat-only reasoning marker stripper and wire it into user-visible output paths</name>
  <read_first>agent-rs/src/main.rs, agent-rs/src/server.rs, agent-rs/src/tui.rs, agent-rs/src/types.rs, .planning/phases/15-fix-blank-ui-output-when-model-streams-tokens/15-CONTEXT.md</read_first>
  <files>agent-rs/src/utils.rs, agent-rs/src/main.rs, agent-rs/src/server.rs</files>
  <behavior>
    - Test 1: Input `"<think>r</think>Hello"` produces `"Hello"` in chat mode.
    - Test 2: Chat mode strips `<think>`, `<thinking>`, `<analysis>` blocks and malformed dangling markers.
    - Test 3: Agentic mode continues to expose thinking traces as before.
  </behavior>
  <action>Create `agent-rs/src/utils.rs` with `strip_reasoning_blocks(text: &str) -> String` implementing marker-family removal for `<think>...</think>`, `<thinking>...</thinking>`, and `<analysis>...</analysis>` using non-greedy-safe strategy plus malformed-tag fallback (per D-04/D-05/D-06). Wire this sanitizer in chat-mode-only paths in `agent-rs/src/main.rs` (terminal/TUI flow before message storage/render output) and `agent-rs/src/server.rs` (web event output path). Keep agentic path behavior unchanged (D-02). Add focused unit tests in `utils.rs` for closed and malformed marker cases.</action>
  <acceptance_criteria>
    - `agent-rs/src/utils.rs` exports `strip_reasoning_blocks` and unit tests for all three marker families.
    - `agent-rs/src/main.rs` conditionally applies `strip_reasoning_blocks` only when execution mode is chat.
    - `agent-rs/src/server.rs` conditionally applies `strip_reasoning_blocks` only for chat-mode output.
    - `cd agent-rs && cargo test -q` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd agent-rs && cargo test -q</automated>
  </verify>
  <done>Chat mode never leaks reasoning markers/content while agentic mode transparency remains intact.</done>
</task>

</tasks>

<verification>
- `cd agent-rs && cargo check -q && cargo test -q`
- Manual chat-mode smoke test: `hey!` produces concise response with no thinking traces.
- Manual regression test: agentic mode still shows existing think/transparency behavior.
</verification>

<success_criteria>
- CHAT-01 covered with explicit mode-specific prompt routing.
- CHAT-02 covered with robust marker stripping in chat-mode display paths.
- No regressions in agentic-mode reasoning visibility behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/15-fix-blank-ui-output-when-model-streams-tokens/15-01-SUMMARY.md`
</output>
